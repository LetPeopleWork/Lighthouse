name: Verify Windows version

on:
  workflow_call:
    inputs:
      version:
        required: true
        type: string

jobs:
  verifywindows:
    runs-on: windows-latest
    if: github.ref == 'refs/heads/main' && github.actor != 'dependabot[bot]'

    steps:
      - name: Download signed Windows binary
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: Lighthouse-win-x64-${{ inputs.version }}
          path: ./publish

      - name: Verify code signature
        shell: powershell
        run: |
          $binary = ".\publish\Lighthouse.exe"
          if (-not (Test-Path $binary)) {
            Write-Error "Error: binary not found at $binary"
            Get-ChildItem .\publish
            exit 1
          }

          Write-Host "Verifying code signature for $binary"
          $signature = Get-AuthenticodeSignature $binary
          
          Write-Host "Signature Status: $($signature.Status)"
          Write-Host "Signer Certificate Subject: $($signature.SignerCertificate.Subject)"
          Write-Host "Signer Certificate Issuer: $($signature.SignerCertificate.Issuer)"
          Write-Host "Timestamp: $($signature.TimeStamperCertificate.NotAfter)"
          
          if ($signature.Status -ne "Valid") {
            Write-Error "Error: Signature status is not Valid"
            exit 1
          }

      - name: Download signed standalone artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: Lighthouse-win-standalone-x64-${{ inputs.version }}
          path: ./publish-standalone

      - name: Verify Installer Signatures
        shell: powershell
        run: |
          $msi = (Get-ChildItem ./publish-standalone -Filter "*_en-US.msi" | Select-Object -First 1).FullName
          $setup = (Get-ChildItem ./publish-standalone -Filter "*-setup.exe" | Select-Object -First 1).FullName

          $targets = @()
          if ($msi) { $targets += $msi }
          if ($setup) { $targets += $setup }

          if ($targets.Count -eq 0) {
              Write-Error "No installers found in ./publish-standalone"
              Get-ChildItem ./publish-standalone
              exit 1
          }

          $failed = $false
          foreach ($path in $targets) {
              Write-Host "`nVerifying: $path"
              $sig = Get-AuthenticodeSignature $path
              
              Write-Host "  Status:  $($sig.Status)"
              Write-Host "  Subject: $($sig.SignerCertificate.Subject)"

              if ($sig.Status -ne "Valid") {
                  Write-Error "Signature invalid for $path"
                  $failed = $true
              }
          }

          if ($failed) { exit 1 }
          Write-Host "`nInstaller signatures verified successfully!"

      - name: Extract and verify portable ZIP signatures
        shell: powershell
        run: |
          $portableZip = Get-ChildItem ./publish-standalone -Filter "*-portable-win-x64.zip" | Select-Object -First 1
          if (-not $portableZip) {
              Write-Error "No portable ZIP found in ./publish-standalone"
              Get-ChildItem ./publish-standalone
              exit 1
          }

          Write-Host "Extracting portable ZIP: $($portableZip.Name)"
          $extractPath = "./publish-portable"
          Expand-Archive -Path $portableZip.FullName -DestinationPath $extractPath -Force

          $exeFiles = Get-ChildItem $extractPath -Filter "*.exe"
          if ($exeFiles.Count -eq 0) {
              Write-Error "No EXE files found in portable ZIP"
              Get-ChildItem $extractPath
              exit 1
          }

          $failed = $false
          foreach ($file in $exeFiles) {
              Write-Host "`nVerifying: $($file.Name)"
              $sig = Get-AuthenticodeSignature $file.FullName

              Write-Host "  Status:  $($sig.Status)"
              Write-Host "  Subject: $($sig.SignerCertificate.Subject)"

              if ($sig.Status -ne "Valid") {
                  Write-Error "Signature invalid for $($file.Name)"
                  $failed = $true
              }
          }

          if ($failed) { exit 1 }
          Write-Host "`nPortable ZIP EXE signatures verified successfully!"

      - name: Run Lighthouse (sqlite)
        shell: bash
        run: |
          cd publish
          ./Lighthouse.exe > lighthouse.log 2>&1 &
          echo $! > lighthouse.pid
          echo "Started Lighthouse with PID $(cat lighthouse.pid)"
        env:
          Database__Provider: sqlite
          Database__ConnectionString: "Data Source=lighthouse.db"

      - name: Wait for Lighthouse to start
        shell: bash
        run: |
          timeout=60
          elapsed=0
          
          echo "Waiting for service to start..."
          while [ $elapsed -lt $timeout ]; do
            if curl -k -s -f http://localhost:5000 > /dev/null 2>&1; then
              echo "Service is up!"
              exit 0
            fi
            
            # Check if process is still running
            if [ -f publish/lighthouse.pid ]; then
              pid=$(cat publish/lighthouse.pid)
              if ! kill -0 $pid 2>/dev/null; then
                echo "Process died unexpectedly!"
                cat publish/lighthouse.log
                exit 1
              fi
            fi
            
            sleep 2
            elapsed=$((elapsed + 2))
          done
          
          echo "Timeout waiting for service"
          cat publish/lighthouse.log
          exit 1

      - name: Stop Lighthouse Server
        if: always()
        shell: bash
        run: |
          if [ -f publish/lighthouse.pid ]; then
            pid=$(cat publish/lighthouse.pid)
            echo "Stopping process $pid"
            kill $pid 2>/dev/null || true
          fi
          
          # Show logs
          echo "=== Lighthouse Log ==="
          cat publish/lighthouse.log || true