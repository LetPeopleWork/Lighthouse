// SCAFFOLD: true
import { describe, it } from "vitest";

describe("BacktestForm — Apply forecast-throughput filter toggle (RED scaffold)", () => {
	it("renders the toggle only on premium tenants where the team has a non-empty filter", () => {
		throw new Error("Not yet implemented — RED scaffold (US-06 AC).");
	});

	it("defaults the toggle to On when visible", () => {
		throw new Error("Not yet implemented — RED scaffold (US-06 AC).");
	});

	it("submitting the form with toggle Off sends applyFilterOverride=false", () => {
		throw new Error("Not yet implemented — RED scaffold (US-06 AC).");
	});

	it("shows the FilteredThroughputChip on the backtest result view when filterApplied=true", () => {
		throw new Error(
			"Not yet implemented — RED scaffold (US-03 chip on backtest result).",
		);
	});
});
