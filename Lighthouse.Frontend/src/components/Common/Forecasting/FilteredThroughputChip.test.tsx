// SCAFFOLD: true
import { describe, it } from "vitest";

describe("FilteredThroughputChip (RED scaffold)", () => {
	it("renders the chip with the 'Filtered throughput' label when visible is true", () => {
		throw new Error(
			"Not yet implemented — RED scaffold (US-03). DELIVER wave: assert chip text and accessible name.",
		);
	});

	it("does not render anything when visible is false", () => {
		throw new Error(
			"Not yet implemented — RED scaffold (US-03 AC: chip absent when filter is not applied).",
		);
	});

	it("shows the excluded summary in the tooltip on hover", () => {
		throw new Error(
			"Not yet implemented — RED scaffold (US-03 AC: tooltip enumerates active rules). DELIVER wave: e.g. 'Type = Bug; Parent Reference ID = (empty)'.",
		);
	});

	it("renders the D5 warning copy when excludedSummary indicates fallback to unfiltered", () => {
		throw new Error(
			"Not yet implemented — RED scaffold (D5 forecast half). DELIVER wave: assert the 'Filter excluded all throughput; showing unfiltered forecast' copy.",
		);
	});
});
