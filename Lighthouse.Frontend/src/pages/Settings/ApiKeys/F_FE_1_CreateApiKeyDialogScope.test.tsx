import { describe, expect, test } from "vitest";

// SCAFFOLD: true
describe("F-FE-1 — CreateApiKeyDialog scope UI", () => {
	test("FE-1.1 collapsed scope section submits without scope field", () => {
		expect.fail("Not yet implemented — RED scaffold");
	});

	test("FE-1.2 single PortfolioRead scope row sends one scope entry", () => {
		expect.fail("Not yet implemented — RED scaffold");
	});

	test("FE-1.3 two scope rows send a scope array of length two", () => {
		expect.fail("Not yet implemented — RED scaffold");
	});

	test("FE-1.4 server 403 keeps dialog open and surfaces error message", () => {
		expect.fail("Not yet implemented — RED scaffold");
	});

	test("FE-1.5 incomplete scope row prevents submission", () => {
		expect.fail("Not yet implemented — RED scaffold");
	});
});
