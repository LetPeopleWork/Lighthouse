<!-- markdownlint-disable MD024 -->
# Feature: state-time-cumulative-view

Epic 4144 (More Detailed State Info) — third and final MVP-bundle feature, covers slice B3 from the Epic catalog.

ADO Epic: <https://dev.azure.com/letpeoplework/Lighthouse/_workitems/edit/4144>
ADO Story (already created): <https://dev.azure.com/letpeoplework/Lighthouse/_workitems/edit/5076>

## Wave: DISCUSS / [REF] Pre-DISCUSS code reality check

Before drafting this delta, the existing Lighthouse charting and metrics surfaces were inspected to confirm or refute the carry-over README's gap claim ("no chart in Lighthouse today answers 'cumulative time per state across items in a window'"). Following the lesson from sibling `aging-pace-percentiles` (where pre-DISCUSS inspection revealed an inaccurate premise), the same rigor was applied here.

**Files inspected**:

- `Lighthouse.Frontend/src/components/Common/Charts/*` — full chart inventory: `BarRunChart`, `BaseRunChart`, `CycleTimePercentiles`, `CycleTimeScatterPlotChart`, `EstimationVsCycleTimeChart`, `FeatureSizeScatterPlotChart`, `LineRunChart`, `LoadBalanceMatrixChart`, `PercentileLegend`, `ProcessBehaviourChart`, `StackedAreaChart`, `TotalWorkItemAgeRunChart`, `TotalWorkItemAgeWidget`, `WorkDistributionChart`, `WorkItemAgingChart`. Plus widgets in `Lighthouse.Frontend/src/pages/Common/MetricsView/`.
- `Lighthouse.Frontend/src/pages/Common/MetricsView/widgetInfoMetadata.ts` — complete widget catalog with descriptions, RAG rules, and learn-more URLs.
- `Lighthouse.Frontend/src/pages/Common/MetricsView/categoryMetadata.ts` — widget placement per category (`flow-overview`, `flow-metrics`, `predictability`, `portfolio`).
- Codebase grep: `timeInState|TimeInState|cumulativeTime|stateBreakdown|cycleTimeByState|timePerState|stateAggregation` — zero production matches in `Lighthouse.Backend` or `Lighthouse.Frontend/src` (only the new feature-delta and journey files for sibling features mention `TimeInState`).
- `Lighthouse.Backend/Lighthouse.Backend/Services/Implementation/TeamMetricsService.cs` and `PortfolioMetricsService.cs` — no per-state aggregation method exists.

**Finding**: the carry-over README's framing is **accurate**. No existing widget or service computes cumulative time-per-state across items in a window. The closest existing widgets and their distinct purposes:

- `stacked` widget — "Simplified Cumulative Flow Diagram showing Doing and Done areas". This is a CFD: areas-OVER-TIME for two `StateCategory` buckets (Doing / Done), not cumulative time-PER-STATE across items. Different chart shape, different question.
- `WorkDistributionChart` — aggregates by parent feature/epic, not by workflow state.
- `WorkItemAgingChart` — per-item dots on a per-state X axis, not aggregated per state.
- `cycleScatter` and `percentiles` widgets — end-to-end cycle time of completed items, not per-state.

**No premise correction required**. The stub README's gap claim stands. The carry-over README is annotated below for reader trust (per Document Update contract: README NOT modified directly; this `feature-delta.md` is the modification record, and a note has been added to the stub README pointing here for clarity).

This contrasts with sibling `aging-pace-percentiles` where the README's premise was wrong (the chart DID have end-to-end CT bands already, gap was only per-state). Surfacing this contrast explicitly so the next DISCUSS reader can trust that BOTH directions (premise-confirmed and premise-corrected) get the same code-reality-check rigor.

## Wave: DISCUSS / [REF] Persona ID

**Primary**: `delivery-lead-rte` — Delivery Lead, Release Train Engineer, Engineering Manager, or anyone owning the team's or release train's quarterly improvement agenda. NEW persona created in this DISCUSS run (`docs/product/personas/delivery-lead-rte.yaml`). Differentiated from `flow-coach`: the flow coach answers "which ITEM is stuck?" (per-item triage, daily/weekly cadence); the delivery lead answers "which STATE is the constraint?" (systemic analysis, quarterly cadence).

**Secondary**: `flow-coach` — same persona file as sibling features; reuses the chart in the retro-facilitation context (a flow coach often co-presents the retro). Decision shape there is also "where to invest improvement effort", but the coach typically frames it per-team and on a sprint cadence, where the delivery lead frames it per-release-train and per-quarter.

## Wave: DISCUSS / [REF] JTBD one-liner

When I am preparing a retrospective, an improvement-planning meeting, or a leadership review and the conversation is about WHERE to invest our process-improvement effort next quarter, I want to see at a glance which workflow state has consumed the most of our collective time during the period — including time that in-flight items are STILL consuming — so I can name the constraint with evidence ("Review took 38% of our cumulative time this quarter") rather than guessing from anecdotes or per-item charts that answer a different question.

**Job-id**: `job-delivery-lead-spot-workflow-constraint` (NEW — added to `docs/product/jobs.yaml` in this DISCUSS run).

Differentiated from the two sibling jobs:

- Sibling A+B1+D (`job-flow-coach-spot-stuck-items`): per-item, "which item is stuck right now?" Triage cadence (daily / weekly). Answer: red badge on a specific work item.
- Sibling F (`job-flow-coach-spot-pace-outliers`): per-item-on-aggregate-bands, "which in-flight item is dragging vs historical pace for its state?" Glance cadence (per chart visit, typically per standup or flow review). Answer: dot above a per-state percentile band.
- THIS feature (`job-delivery-lead-spot-workflow-constraint`): per-state-on-aggregate, "which workflow STATE took the most of our collective time?" Systemic cadence (retro, quarterly planning, leadership review). Answer: the tallest bar on a bar chart of cumulative time per state.

The three jobs all consume the same `WorkItemStateTransition` data foundation but answer materially different questions. None is a substitute for the others — they are complementary lenses on the same underlying flow data.

## Wave: DISCUSS / [REF] Locked decisions

| ID | Decision | Verdict |
|---|---|---|
| D1 | NO existing cumulative time-per-state chart in Lighthouse. Verified via pre-DISCUSS code reality check above. No premise correction needed; the carry-over README's gap claim stands. | Locked — confirmed via inspection of `Charts/*`, `widgetInfoMetadata.ts`, `categoryMetadata.ts`, and backend metrics services. |
| D2 | **Default chart placement**: team detail page AND portfolio detail page, both under the existing `flow-metrics` category. Widget key: `stateTimeCumulative`. Size: `large`. Same placement reasoning as sibling F (D8): scope parity avoids confusion. Shipping a new top-level route would fragment the user's mental model. | Locked. |
| D3 | **Bar ordering**: workflow order (left-to-right matching the team's kanban board, same ordering as `WorkItemAgingChart` X-axis state columns). Alternatives considered: descending-by-cumulative-time (rejected — breaks pipeline-shape mental model); by `StateCategory` (rejected — hides per-state detail which is the whole point). | Locked. |
| D4 | **Unit displayed**: total time (in days) as the primary bar height. Tooltip discloses BOTH total and mean-per-item plus the item count. No user-facing total/mean toggle in MVP — one chart, one story. If post-release telemetry shows the mean view as a recurring user need, fold into a follow-up. | Locked. |
| D5 | **Date-range semantics: full-duration attribution with frame-based item selection** (revised 2026-05-24 per user redirect). The filter window selects WHICH ITEMS are included (per D12 inclusion rule); for every included item, the bar counts the FULL time the item spent in that state — regardless of whether that time falls inside or outside the window. An item that entered "Review" Dec 1 2025 and exited Jan 20 2026 contributes its FULL 50 days to the Review bar, even when the window is Q1 2026. Rationale: the chart's job is to surface the *real cycle pattern* of items relevant to the period, not to clip duration accounting to the window. Clipping would hide actual time items spent in slow states and mislead leadership about the true cost of those states. User verbatim (2026-05-24): *"we pick the items that were relevant within the frame, but then look at the full time. so even if an item was closed on the 1st day of the window, if it was in progress 40 days before that, we should count the full time."* **Alternative considered and rejected**: clip-to-window (was the initial DISCUSS default; rejected on user redirect because it under-counts real cycle time and conceals the constraint). A future toggle (full vs clip) is out-of-scope for MVP unless post-release telemetry shows demand. | Locked. |
| D6 | **Ongoing-items visual treatment**: each bar is stacked into TWO segments — a solid base segment for "completed contribution" (items that exited the state during the window) and a diagonally-hatched top segment for "ongoing contribution" (items still in the state at window end). User-stated requirement verbatim 2026-05-24: *"cumulative times for all items in the filter, including ongoing items"*. Hatching is the familiar Lighthouse "in progress" visual idiom (consistent with how blocked items are emphasized today). Alternatives considered: different shade (rejected — colour-blind users would lose the distinction); separate bar per segment (rejected — doubles the chart's bar count and breaks the one-bar-per-state mental model). | Locked. |
| D7 | **Filter composition**: composes UNCHANGED with existing Team/Portfolio scope, work-item-type chips, and date-range selector. No new filter primitives. Chart recomputes when any of those change. Matches sibling F's D7. | Locked. |
| D8 | **WS strategy**: Type A (additive). No contract change to existing endpoints. ONE new endpoint per scope (team + portfolio); ONE new chart widget; ONE new entry in `widgetInfoMetadata` + `categoryMetadata`. If the endpoint is absent, the chart slot is empty — no regression to existing widgets. | Locked. |
| D9 | **Sibling-data dependency on `WorkItemStateTransition`**: this DISCUSS does NOT request any schema addition to the transition table. The four fields shipped in sibling slice 01 (`workItemId`, `fromState`, `toState`, `transitionedAt`) PLUS the derived `currentStateEnteredAt` field on `WorkItem` are sufficient. The cumulative-time-per-state computation is fully expressible from those alone (formula in `journey-state-time-cumulative-view.yaml` D9 entry). **No upstream coordination with sibling 1 DESIGN required.** | Locked — explicit no-impact on sibling DESIGN. |
| D10 | **Cross-feature alignment with sibling F**: both this feature and `aging-pace-percentiles` compute per-state aggregations from `WorkItemStateTransition`. A shared per-state aggregation service / materialised cache is a candidate DESIGN-time optimization but NOT required for MVP correctness. MVP proceeds assuming independent computation per feature; DESIGN may consolidate if profiling demands it. | Locked (DESIGN-flagged, not DISCUSS-blocking). |
| D11 | **In-flight items MUST contribute**: user-stated requirement verbatim 2026-05-24. The bar MUST include time-in-current-state for items still in flight at window end (provided they meet D12's inclusion rule). Per D5 full-duration semantics, an in-flight item's contribution to the ongoing segment is its FULL `now - currentStateEnteredAt` time (not clipped to windowStart). D6 covers the visual treatment. | Locked (verbatim user requirement). |
| D12 | **Item-inclusion rule**: a work item is included in the chart if (a) its timeline has at least one `(state-enter, state-exit)` interval whose `[stateEnter, stateExit]` window intersects the filter window, OR (b) it is currently in-flight with `currentStateEnteredAt ≤ windowEnd`. Concretely covers: items closed within the window, items started within the window, items in any non-Done state at any point during the window, items still in-flight today whose current state was entered before windowEnd. Once included, the item's FULL state-durations contribute (per D5). Items entirely outside the window (e.g. closed before windowStart with no state-time overlap) are excluded. | Locked (user-confirmed 2026-05-24). |
| D13 | **US-03 withdrawn** (revised 2026-05-26 per user). The standalone tooltip "Included items: A closed, B still in flight" line is dropped — the per-state drill-down (US-04, click-to-open item table) is the stronger "which items?" affordance and makes the explanatory line redundant. The completed/ongoing item COUNTS remain in US-01's tooltip (they are already in the endpoint payload and US-01 AC). The inclusion+attribution EXPLANATION (D5/D12 in plain language) moves to the `stateTimeCumulative` entry in `widgetInfoMetadata.ts` (learn-more text) — one discoverable place, screen-reader reachable, instead of on every hover. User verbatim (2026-05-26): *"US-03 seems like it's not needed - we can click to drill down."* | Locked. |
| D14 | **In-chart item filter (US-05, new 2026-05-26 per user)**: a multi-select item picker on the chart lets the user scope the bars from the systemic default ("all cumulated") down to a chosen subset ("see it for a single or a set of items, e.g. all stories related to a feature"). The picker **searches by Reference ID or Name only** (other work-item attributes are not search keys — user-specified) and supports **parent-expand**: matching a parent (feature/epic) Reference ID offers "select all its children", so the "all stories of a feature" case is one action. Default = no selection = systemic all-items view (unchanged). User verbatim (2026-05-26): *"a filter on the chart itself to select specific item(s), and then we see the distribution for those … General: see all cumulated. Filtered: see it for a single or a set of items."* The filter answers the "where did THESE item(s) spend time?" question alongside the systemic "where does the team spend time?" question — the transpose of US-04 (US-04 fixes a state and lists items; US-05 fixes items and shows their states). | Locked. |
| D15 | **Feature B2 absorbed (distribution only; chronology dropped)** (2026-05-26 per user). With US-05's picker narrowed to a single item, the chart already shows that item's per-state time *distribution* — which is the substantive value of former post-MVP feature B2 (`work-item-state-history-view`, "per-item outlier deep-dive"). B2 is therefore **retired from Epic 4144**: its distribution lens lands here via US-05; its *chronology* lens (ordered transition timeline, re-entry sequence with dates) is **explicitly dropped for MVP** — revisit as a follow-up only if telemetry/users ask. Consequence: this feature gains a secondary persona/job (Product Owner outlier deep-dive — `product-owner` persona + `job-po-deep-dive-item-state-time`). User verbatim (2026-05-26): *"Absorb distribution, drop chronology."* Epic catalog (`epic-4144-more-detailed-state-info/README.md`) updated to remove B2. | Locked. |
| D16 | **Adaptive uniform display unit** (2026-05-26 per user). Bar height is computed at full calendar-time precision; the DISPLAYED unit is chosen ONCE per chart render from the largest bar (ladder: minutes → hours → days → weeks) and applied uniformly to every bar so heights stay visually comparable. Rationale: a fixed "days" unit reads `0.3d` for a single-item / short-window view and is unreadable; "100 items over 30 days" wants days or weeks. Tooltip may disclose a finer secondary value. Time is wall-clock/calendar (NOT business-hours — matches existing cycle-time/age charts; business-hours calendars are a separate large feature, out of scope). The exact backend contract unit (precise days-double vs seconds) is a DESIGN detail; the DISCUSS decision is "precise compute, adaptive uniform display, calendar time." User verbatim (2026-05-26): *"display something based on the scale … overall we have to account for differences in scale."* | Locked. |
| D17 | **Date window gates the picker candidate set** (2026-05-26 per user, supersedes an earlier proposed default). The active date window (+ ambient team/portfolio/work-item-type filters) determines which items are D12-included; the US-05 picker selects ONLY from that windowed candidate set. An item not touched within the window is not findable in the picker. Selecting a subset narrows which items the bars are computed over; full-duration attribution (D5) still applies to whatever is selected. User verbatim (2026-05-26): *"The date window will select which items are even available. From this filtered set, you can now search/select. If the item wasn't touched in the original time window, you won't find it via the filter on the chart."* | Locked. |
| D18 | **RAG reflects the whole in-scope set, never the picker selection** (2026-05-26 per user). The widget's RAG status (`computeCumulativeStateTimeRag`, 40%/60% thresholds) is computed over ALL D12-included items for the current ambient filters and is NOT recomputed when the US-05 picker narrows the displayed bars — identical to how hiding a work-item-type in the cycle-time chart leaves its RAG unchanged. The picker is a view-level lens, not a recompute of the systemic health signal. User verbatim (2026-05-26): *"RAG should only apply to whole set … same behaviour if I would hide a specific work item type in the cycle time chart - it doesnt change the rag."* | Locked. |

## Wave: DISCUSS / [REF] User stories with elevator pitches

### US-01 — Cumulative time-per-state bar chart for filtered items (team scope, walking skeleton)

**Story**: As a `delivery-lead-rte`, I want a horizontal bar chart on my team's detail page (under Flow Metrics) showing total time spent in each workflow state across all items currently in my filter — INCLUDING time-in-current-state for in-flight items — so I can see at a glance which workflow state is the constraint on my team's throughput this quarter.

**Job-id**: `job-delivery-lead-spot-workflow-constraint`

#### Elevator Pitch

Before: I want to know "where did our team's time go last quarter?" but no chart in Lighthouse answers it. The Simplified CFD shows volume-over-time by `StateCategory` (Doing/Done only), not per workflow state, not aggregated. To answer the question I either guess (anecdote from the retro), or I export raw history into a spreadsheet and pivot it myself.
After: open `/teams/{teamId}` → Flow Metrics tab → see a new "Cumulative Time per State" widget. Bars are one-per-state in workflow order (matching the team's kanban). Each bar is split into a solid base (time contributed by items completed during the window) and a diagonally-hatched top segment (time contributed by items still in that state at window end). Hover a bar → tooltip shows `State: Review | Total: 184d | Items: 47 (38 completed, 9 ongoing) | Mean: 3.9d`.
Decision enabled: which workflow state to target for next quarter's process-improvement investment — "Review consumed 38% of our time last quarter; that's where the improvement budget goes".

**AC**:

- Given a team with `WorkItemStateTransition` data accumulated and at least one item matching the active filter AND the D12 inclusion rule, when I open `/teams/{teamId}` and switch to the Flow Metrics category, then the `stateTimeCumulative` widget renders with one bar per workflow state in the team's workflow order.
- Each bar's height equals the sum, over all D12-included items, of (a) the FULL duration `stateExit - stateEnter` of every completed `(enter, exit)` interval that item had in this state PLUS (b) `now - currentStateEnteredAt` if the item is currently in this state — per D5 full-duration attribution. The window does NOT clip durations; it selects which items are summed.
- Each bar is visually stacked into a solid base segment (`completedContribution`: time from D12-included items that have exited the state by now) and a diagonally-hatched top segment (`ongoingContribution`: time from items still in the state at now) — per D6.
- Bar tooltip on hover shows: state name, total days, item count, completed-contribution days, ongoing-contribution days, mean days per item, median days per item, and partial-window-item count.
- Given the filter changes (date range, work-item-type chip, blocked toggle), the chart refetches and re-renders with the recomputed bars — no manual refresh; same lifecycle as existing Flow Metrics widgets.
- Given an empty filter (no items match), the widget renders an empty-state message identical in tone to existing `WorkDistributionChart` and `WorkItemAgingChart` empty states.
- Given a workflow state with zero contributing items, that state's bar renders as a labeled placeholder with height 0 (so users see which states are part of the workflow definition, not "did the chart drop a state?").
- The widget's RAG rule (computed via a new `computeCumulativeStateTimeRag` function in `ragRules.ts`) maps to status guidance in `widgetInfoMetadata.ts`: `sustain` when one state has ≤ 40% of total cumulative time (balanced flow); `observe` when one state has 40-60%; `act` when one state has > 60% (single dominant constraint). Per D18, the RAG is computed over the whole in-scope set and is NOT recomputed when the US-05 picker narrows the displayed bars.
- Bar heights are displayed in an adaptive unit chosen once per render from the largest bar (minutes → hours → days → weeks) and applied uniformly to every bar so heights stay comparable (D16). Underlying values are computed at full calendar-time precision; the chosen unit is labelled on the axis/legend and the tooltip may show a finer secondary value. A single-item or short-window view that would read `0.3d` instead reads in hours/minutes.
- No regression in the rest of the Flow Metrics category: existing widgets render unchanged when the new widget is absent.

### US-02 — Cumulative time-per-state bar chart on the portfolio detail page

**Story**: As a `delivery-lead-rte` running a portfolio-level improvement conversation, I want the same Cumulative Time per State chart on the portfolio detail page, so I can name constraints at the portfolio level the same way I name them at team level — without per-scope parity holes.

**Job-id**: `job-delivery-lead-spot-workflow-constraint`

#### Elevator Pitch

Before: even if the chart ships at team scope, portfolio-level retros and leadership reviews would have to derive the answer from per-team data and add it up manually — defeating the integrated-tool advantage and breaking parity with how the existing Flow Metrics category treats both scopes uniformly.
After: open `/portfolios/{portfolioId}` → Flow Metrics tab → see the same `stateTimeCumulative` widget, computed across all work items in the portfolio's scope (subject to the portfolio's existing filter primitives). Bar geometry and tooltip identical to the team-scope chart, only the scope differs.
Decision enabled: portfolio-level improvement priorities — "across all teams in this portfolio, Review is the bottleneck; let's prioritise Review-process work train-wide" — rather than "per-team Reviews are messy, let me eyeball it".

**AC**:

- Given a portfolio with `WorkItemStateTransition` data accumulated for its constituent work items, when I open `/portfolios/{portfolioId}` and switch to the Flow Metrics category, then the `stateTimeCumulative` widget renders with the same shape and behavior as the team-scope chart (US-01), scoped to the portfolio's work items.
- The portfolio-scope endpoint shares the request/response contract with the team-scope endpoint (same JSON shape, only the route prefix differs).
- All US-01 AC items (empty state, zero-contributing state, tooltip content, RAG rule, filter composition) hold for the portfolio scope unchanged.
- No regression in the rest of the portfolio's Flow Metrics category.

### US-03 — WITHDRAWN (2026-05-26, per D13)

This story (a dedicated tooltip "Included items: A closed, B still in flight" line) is **withdrawn**. The per-state drill-down (US-04, click-to-open item table) is the stronger "which items?" affordance and makes the explanatory line redundant. What US-03 carried is preserved:

- The completed/ongoing item **counts** stay in US-01's tooltip (already in the endpoint payload and US-01 AC).
- The inclusion+attribution **explanation** (D5/D12 in plain language) moves to the `stateTimeCumulative` entry in `widgetInfoMetadata.ts` learn-more text — discoverable once, screen-reader reachable.

The ID is retained (not renumbered) so ADO #5076 and the DESIGN-wave references stay anchored. No endpoint or contract change resulted from the withdrawal.

### US-04 — Per-item drill-down on bar click

**Story**: As a `delivery-lead-rte`, when I identify a state as the constraint from the bar chart, I want to click that state's bar to see a table of the items that contributed and exactly how many days each contributed, so I can name the specific items driving the constraint when I raise it in a retrospective or improvement-planning meeting.

**Job-id**: `job-delivery-lead-spot-workflow-constraint`

#### Elevator Pitch

Before: the bar tells me "Review took 38% of our cumulative time this quarter", but I can't see *which items* drove that. To name names I would have to open each item by hand, read its state history, sum the days, and pivot in my head — friction that makes the chart a conversation-starter but not a conversation-driver.
After: click on the "Review" bar → a per-item panel opens listing each contributing item with `ID · Title · Type · Current State · Days contributed to Review`, sorted by days descending by default. The top 3 rows usually account for most of the bar; that's the retro agenda.
Decision enabled: which specific items to raise in the retro / improvement meeting ("Items #1234, #1289, and #1301 account for 70% of Review time this quarter — let's discuss those three").

**AC**:

- Given the chart is rendered (US-01 or US-02), when I click on a state's bar (or its labelled placeholder for zero-contributing states), then a per-item panel opens (implementation choice between modal dialog and expandable side-panel deferred to DESIGN).
- The panel's table has columns: Work Item ID (linkable to the existing work-item detail view), Title, Work-Item Type, Current State, Days Contributed To Selected State (the per-item portion of the bar height — full-duration per D5).
- Default sort: Days Contributed To Selected State, descending.
- Sorting by any column is supported (click column header to toggle asc/desc).
- The sum of "Days Contributed" across all rows in the panel equals the bar's height for that state (within ±0.1d rounding tolerance — sanity-check assertion in integration test).
- Closing the panel (Escape key, outside click, or explicit close control) returns to the chart view with the previous filter state intact.
- Keyboard accessible: Escape closes; Tab/Shift+Tab navigates rows; Enter on a Work Item ID row activates the work-item link.
- ARIA: panel has `role="dialog"` (or appropriate landmark for an expandable side-panel), labelled with "Items contributing to {state name}".
- Filter respect: the table reflects the active filter (type chips, date range, scope). Changing the filter while the panel is open re-fetches the per-item data (consistent with the chart's live-recompute lifecycle in US-01 AC).
- Zero-contributing case: clicking a zero-contributing-state bar opens an empty panel with the message "No items contributed to this state in the selected window."
- No regression: existing chart click handlers (other widgets in the Flow Metrics category) are unaffected.

### US-05 — Scope the chart to a selected item or set of items (in-chart item picker)

**Story**: As a `delivery-lead-rte` (or a `product-owner` doing an outlier deep-dive), I want a multi-select item picker on the chart so I can narrow the bars from "all items cumulated" down to a single item or a chosen set (e.g. all stories under one feature), so I can see where THOSE items spent their time — alongside the systemic view — without leaving the chart.

**Job-id**: `job-delivery-lead-spot-workflow-constraint` (subset-of-a-feature lens) AND `job-po-deep-dive-item-state-time` (single-item outlier deep-dive — absorbed from former feature B2, per D15)

#### Elevator Pitch

Before: the chart answers "where does the whole team spend its time?" but not "where did *this feature's stories* spend theirs?" or "this one outlier item took 6 weeks — where did its time go?". Today that means exporting state history and pivoting in a spreadsheet, the exact friction the chart was meant to remove.
After: open the chart → use the **item picker** (search by Reference ID or Name; match a parent feature to "select all its children") → pick one or many items → the same bars recompute over just that selection. Clear the picker → back to the systemic all-items view. The displayed unit adapts (a single item over a few days reads in hours, not `0.0d`).
Decision enabled: for a feature retro — "across this feature's 8 stories, Review ate 60% of the time"; for an outlier post-mortem — "this item sat in Review 31 of its 44 days — that's the story to tell".

**AC**:

- Given the chart is rendered (US-01/US-02), then an item picker control is available that lists the D12-included items for the current window + ambient filters (per D17 — items not touched in the window are not selectable).
- The picker supports search matching **Reference ID or Name only**; typing filters the candidate list. No other attributes are search keys (D14).
- Parent-expand (D14): when the search matches a parent (feature/epic) Reference ID, the picker offers "select all its children", adding all child items currently in the candidate set in one action.
- With one or more items selected, the chart recomputes the bars over ONLY the selected items, applying full-duration attribution (D5) and the completed/ongoing segmentation (D6) to that subset; the adaptive display unit (D16) is re-chosen for the narrowed magnitude.
- With the picker cleared (no selection), the chart shows the systemic all-items view unchanged (this is the default).
- Per D18, the widget RAG status continues to reflect the WHOLE in-scope set and does NOT change when the picker narrows the displayed bars.
- The per-state drill-down (US-04) composes with an active selection: clicking a bar lists only the selected items that contributed to that state.
- A single-item selection renders correctly (one item's per-state distribution; this is the absorbed B2 deep-dive lens — D15); chronology/ordered-transition view is explicitly NOT provided (out of scope, D15).
- Picker state is local to the chart view and resets on navigation away; it does not mutate the page-level ambient filters.
- Picker is keyboard accessible (open/close, type-to-search, multi-select with keyboard, parent-expand action reachable) and screen-reader labelled.
- Empty candidate set (no D12-included items for the window): the picker renders a disabled/empty state consistent with the chart's own empty state.

## Wave: DISCUSS / [REF] Definition of Done

1. All active stories (US-01, US-02, US-04, US-05; US-03 withdrawn per D13) pass their ACs via integration tests (NUnit + EF InMemory + WebApplicationFactory for the new endpoints; Vitest + React Testing Library for the chart widget, drill-down panel, and item picker).
2. Cumulative time-per-state math (D5 full-duration) verified against a fixture: a known set of items with known (state-enter, state-exit) intervals AND known in-flight items, asserting exact bar heights AND exact completed/ongoing segment heights for each state; verifying that the FULL state durations contribute regardless of whether they fall inside or outside the window.
3. Date-range INCLUSION semantics (D5, D12) verified against a fixture: items closed inside the window, items closed on day 1 of the window with pre-window state-time, items in-flight throughout the window, items still in flight today, items fully outside the window (must NOT be included). Assert exact contribution per included item.
4. Ongoing-item attribution (D6, D11) verified: in-flight items contribute their FULL `now - currentStateEnteredAt` time to the ongoing segment for their current state (no clipping to windowStart).
5. Filter composition (D7) verified: changing a type chip, date range, or scope re-fetches and re-renders correctly; no stale-cache bugs (this is why `bug-5016-cache-thread-safety` is a pre-req — same reason sibling 1 listed it).
6. Empty-state, zero-contributing-state, single-item, and state-removed-from-workflow edge cases all render gracefully per AC — for both the chart and the drill-down panel.
7. Portfolio-scope parity (US-02) verified by integration test asserting both scopes return shape-identical responses for the same underlying items.
7a. Per-item drill-down (US-04) verified: integration test asserts the panel's per-item rows for a clicked state sum (within ±0.1d) to that state's bar height; Vitest + RTL test asserts keyboard accessibility (Escape closes, Tab navigates), ARIA labelling, default sort, column-sort toggling, and filter-respect re-fetch behaviour.
7b. Item picker (US-05) verified: integration test asserts candidate items are exactly the D12-included set for the window (D17), Reference-ID/Name search filters them, parent-expand selects all children, and the bars recompute over only the selection (full-duration per D5, segmentation per D6). Vitest + RTL test asserts the systemic-default-when-cleared behaviour, that RAG is unchanged by selection (D18), single-item rendering (absorbed B2 distribution lens — D15), and keyboard/SR accessibility of the picker.
7c. Adaptive units (D16) verified: a Vitest test on the unit-formatting util asserts the chosen unit is driven by the largest bar (minutes → hours → days → weeks), is uniform across bars, and that a sub-day single-item magnitude renders in hours/minutes rather than `0.x d`.
8. No regression in existing Flow Metrics widgets (`computeSimplifiedCfdRag`, `computeWorkItemAgeChartRag`, etc.); existing `ragRules.test.ts` passes unchanged.
9. `dotnet build` zero warnings; `pnpm build` clean (CI parity per CLAUDE.md).
10. SonarCloud quality gate passes on PR.
11. Mutation testing (Stryker.NET for Backend; Stryker for Frontend): ≥80% kill rate for new code.
12. Docs updated: screenshot of the chart with both segments visible and a callout for the new widget; `widgetInfoMetadata.ts` learn-more URL points to a documentation page added in the same wave.

## Wave: DISCUSS / [REF] Out of scope

- **Total/mean per-item toggle in the widget** — deferred (D4). MVP ships total only; mean is in the tooltip. Fold into a follow-up if telemetry warrants.
- **Runtime toggle between full-duration and clip-to-window semantics** — out of scope. D5 locks full-duration per user redirect on 2026-05-24; a toggle would only ship if post-release telemetry shows demand from leadership-review users who want both lenses.
- **Comparing two windows side-by-side (e.g. this quarter vs last quarter on one chart)** — out of scope. The user is expected to flip the date range OR open two browser tabs. A dedicated comparison view would be a separate feature.
- **Per-item state CHRONOLOGY (ordered transition timeline, re-entry sequence with dates)** — out of scope (D15). Former feature B2's *distribution* lens (where did this item's time go, per state) is now IN scope via the US-05 single-item picker selection; only its chronological/timeline lens is dropped, revisited as a follow-up only if asked. US-05 sums re-entries of a state into one bar (cumulative), deliberately not showing the order they happened in.
- **Business-hours / working-calendar time** — out of scope (D16). Time is wall-clock/calendar, matching existing cycle-time and age charts. A working-hours calendar is a separate large feature.
- **Configurable adaptive-unit override (force days/hours)** — out of scope. The unit is auto-chosen per render (D16); a manual override folds into a follow-up only on telemetry signal.
- **Cross-team or cross-portfolio aggregation** — out of scope; chart is scoped to one team or one portfolio at a time. Multi-scope is a future feature.
- **Configurable bar ordering** — out of scope. D3 picks workflow order; if users ask for descending-by-time, fold into a follow-up.
- **Configurable RAG thresholds for the new widget** — out of scope. The 40%/60% thresholds in the RAG rule are baseline defaults; if customer feedback warrants tunability, fold into a follow-up alongside the existing per-widget RAG-config feature work.
- **Changes to existing `WorkItemStateTransition` schema** — D9 explicitly preserves it. Sibling 1's DESIGN is unaffected.
- **Backfill of historical transitions before sibling slice 01 shipped** — sibling locked forward-only; this feature inherits the same constraint. The chart will be sparse for the first weeks post-release until enough transitions accumulate.
- **Time-in-blocked attribution** — blocked-time history is Epic #5074, separate mechanism. The bar chart shows time-in-state regardless of blocked status; when Epic #5074 ships, a blocked-vs-non-blocked breakdown of each bar could be added.
- **Shared per-state aggregation service with sibling F** — D10 deferred to DESIGN-time coordination, not DISCUSS scope.

## Wave: DISCUSS / [REF] WS strategy

**Type A (additive walking skeleton).** No contract change to existing endpoints. Two new endpoints per scope (one for the bar data, one for the per-state drill-down items), one new chart widget, one new drill-down panel component, one new item-picker control, one new widget-metadata entry, one adaptive-unit formatting util. Walking skeleton = US-01 against the team scope only, with the new bar-data endpoint, the new widget rendering in the Flow Metrics category, the adaptive display unit (D16), and the empty / zero-state edge cases handled. US-02 = portfolio-scope parity (same chart + same endpoint shape, scope-swap). ~~US-03~~ withdrawn (D13) — the completed/ongoing counts already piggy-back on the US-01 response and stay in the tooltip; the inclusion explanation lives in widget-info metadata. US-04 = drill-down panel + new per-state items endpoint (kept separate from the bar-data endpoint to keep the bar-chart payload small for the common case where users only glance at the chart). US-05 = in-chart item picker (search by Ref ID/Name + parent-expand) scoping the bars to a selected subset; the bar-data and drill-down endpoints accept an optional item-id list (selection bypasses no inclusion rule — picker candidates are already the D12-included set per D17), and a candidate-listing endpoint per scope feeds the picker's searchable list.

## Wave: DISCUSS / [REF] Driving ports

| Method | Route | Auth | Status | Change |
|---|---|---|---|---|
| GET | `/api/teams/{teamId}/metrics/cumulativeStateTime?startDate&endDate&itemIds` | Authenticated | **New** | Returns `{ states: [{ state: string, workflowOrder: int, totalDays: double, completedContributionDays: double, ongoingContributionDays: double, itemCount: int, completedItemCount: int, ongoingItemCount: int, meanDays: double, medianDays: double }] }` — one entry per workflow state, ordered by `workflowOrder`. Durations are full-precision (per D16 the frontend picks the display unit); `totalDays` etc. remain the contract's canonical numeric (exact contract unit is a DESIGN detail). Item counts (`completedItemCount`, `ongoingItemCount`) feed US-01's tooltip. Optional `itemIds` (US-05) narrows the computation to the selected subset; absent = all D12-included items. Empty `states` array when no items match. |
| GET | `/api/portfolios/{portfolioId}/metrics/cumulativeStateTime?startDate&endDate&itemIds` | Authenticated | **New** | Same shape as the team route, scoped to the portfolio (D2). Optional `itemIds` per US-05. |
| GET | `/api/teams/{teamId}/metrics/cumulativeStateTime/items?state=X&startDate&endDate&itemIds` | Authenticated | **New (US-04)** | Returns `{ state: string, items: [{ workItemId: string, referenceId: string, title: string, workItemType: string, currentState: string, daysContributed: double }] }` — per-item breakdown for ONE state. Ordered by `daysContributed` descending (default sort matches the panel's default; client may re-sort). Optional `itemIds` (US-05) restricts to the selected subset. Empty `items` array when no items contributed. Kept separate from the bar endpoint to keep the chart payload small for users who never drill down. |
| GET | `/api/portfolios/{portfolioId}/metrics/cumulativeStateTime/items?state=X&startDate&endDate&itemIds` | Authenticated | **New (US-04)** | Same shape as the team route, portfolio-scoped. |
| GET | `/api/teams/{teamId}/metrics/cumulativeStateTime/candidates?startDate&endDate` | Authenticated | **New (US-05)** | Returns `{ items: [{ workItemId: string, referenceId: string, title: string, workItemType: string, parentReferenceId: string? }] }` — the D12-included items selectable in the picker for the window (D17). `referenceId` + `title` are the search keys (D14); `parentReferenceId` drives parent-expand. Feeds the picker's searchable list so the chart never has to ship every item's data up front. |
| GET | `/api/portfolios/{portfolioId}/metrics/cumulativeStateTime/candidates?startDate&endDate` | Authenticated | **New (US-05)** | Same shape, portfolio-scoped. |
| GET | `/api/teams/{teamId}/metrics/cycleTimePercentiles` | Authenticated | Existing | **Unchanged** (D9). |
| GET | `/api/teams/{teamId}/metrics/ageInStatePercentiles?startDate&endDate` | Authenticated | New-via-sibling-F | **Unchanged** by THIS feature; sibling F ships it independently. |

UI surfaces touched:

- `Lighthouse.Frontend/src/components/Common/Charts/CumulativeStateTimeChart.tsx` (NEW): horizontal-bar chart widget with stacked completed/ongoing segments per bar, tooltip per D5/D6 (completed/ongoing counts retained; inclusion explanation in widget-info per D13), adaptive display unit per D16, click-to-drill-down handler per US-04, item-picker integration per US-05.
- `Lighthouse.Frontend/src/components/Common/Charts/CumulativeStateTimeDrillDownPanel.tsx` (NEW): per-item table opened from a bar click; columns / sort / accessibility per US-04 AC. Implementation choice (modal vs side panel) deferred to DESIGN.
- `Lighthouse.Frontend/src/components/Common/Charts/CumulativeStateTimeItemPicker.tsx` (NEW, US-05): multi-select picker with Ref-ID/Name search and parent-expand; emits the selected item-id set the chart passes as `itemIds`. Default cleared = systemic view. Accessibility per US-05 AC.
- `Lighthouse.Frontend/src/utils/duration/formatDuration.ts` (NEW, D16): chooses one display unit (minutes → hours → days → weeks) from a magnitude and formats uniformly; used for bar labels, axis, and tooltip. (Exact path a DESIGN detail; co-locate with existing formatting utils if one exists.)
- `Lighthouse.Frontend/src/pages/Common/MetricsView/categoryMetadata.ts`: add `{ widgetKey: "stateTimeCumulative", size: "large" }` to `flow-metrics`.
- `Lighthouse.Frontend/src/pages/Common/MetricsView/widgetInfoMetadata.ts`: add `stateTimeCumulative` entry with description, learn-more URL, and RAG status guidance per US-01 AC.
- `Lighthouse.Frontend/src/pages/Common/MetricsView/ragRules.ts`: add `computeCumulativeStateTimeRag` function returning sustain/observe/act per the AC's % thresholds.
- `Lighthouse.Frontend/src/pages/Common/MetricsView/BaseMetricsView.tsx`: import and dispatch the new widget by `widgetKey` (mirror existing `WorkDistributionChart` / `WorkItemAgingChart` dispatch pattern).
- `Lighthouse.Frontend/src/services/Api/MetricsService.ts` (and the matching interface): add `getCumulativeStateTimeForTeam(teamId, startDate, endDate, itemIds?)`, `getCumulativeStateTimeForPortfolio(portfolioId, startDate, endDate, itemIds?)`, `getCumulativeStateTimeItemsForTeam(teamId, state, startDate, endDate, itemIds?)`, `getCumulativeStateTimeItemsForPortfolio(portfolioId, state, startDate, endDate, itemIds?)`, and the US-05 candidate-listing methods `getCumulativeStateTimeCandidatesForTeam(teamId, startDate, endDate)` / `…ForPortfolio(portfolioId, startDate, endDate)`.
- `Lighthouse.Backend/Lighthouse.Backend/Services/Implementation/TeamMetricsService.cs` and `PortfolioMetricsService.cs`: add `GetCumulativeStateTimeForTeam` / `GetCumulativeStateTimeForPortfolio` methods (read `WorkItemStateTransition` rows + `WorkItem.currentStateEnteredAt`, apply D12 inclusion rule, optional `itemIds` subset filter per US-05, sum FULL durations per D5, return the response shape above) plus `GetCumulativeStateTimeItemsFor{Team|Portfolio}` (per-state per-item rows) and `GetCumulativeStateTimeCandidatesFor{Team|Portfolio}` (D12-included selectable items for the picker, per D17).
- `Lighthouse.Backend/Lighthouse.Backend/API/TeamMetricsController.cs` and `PortfolioMetricsController.cs`: add the six new endpoints (bar data, drill-down items, picker candidates — each per scope).

No new top-level routes; no schema additions. The RAG computation (`computeCumulativeStateTimeRag`) runs on the full in-scope set and ignores the `itemIds` selection (D18).

## Wave: DISCUSS / [REF] Pre-requisites

- **HARD**: `time-in-state-and-staleness` slice 01 must be **merged and shipped** so `WorkItemStateTransition` exists and connector capture is running for transitions. (Sibling 1 DISCUSS is complete; DESIGN/DELIVER not yet started — same MVP-bundle dependency note as sibling F.)
- **HARD**: `bug-5016-cache-thread-safety` (merged 2026-05-17) — the metrics cache must be thread-safe before adding a new derived metric that flows through it (same reasoning as sibling 1).
- **MEDIUM**: sibling F (`aging-pace-percentiles`) DESIGN — only relevant for the cross-feature coordination decision (D10). If F's DESIGN decides to ship a shared per-state aggregation service, this feature's DESIGN should consume it; if F's DESIGN computes independently, this feature does the same.
- The existing Flow Metrics category infrastructure (`BaseMetricsView`, `categoryMetadata`, `widgetInfoMetadata`, `ragRules`) is in place — confirmed in pre-DISCUSS code reality check.

**MVP bundle dependency note**: this feature cannot enter DELIVER until sibling 1 (`time-in-state-and-staleness`) is at least merged to main (the transitions table must exist and connector capture must be running). DESIGN of this feature CAN run in parallel with sibling 1's DELIVER and sibling F's DESIGN. This sequencing is consistent with sibling 1's locked D3 (`Slice ordering across Epic 4144: A+B1+D → F → B2 → B3 → C`) — though in practice the MVP bundle ships all three together at the end of B3.

No DISCOVER or DIVERGE artifacts exist for Epic 4144; this DISCUSS run extends the same "community-validated via Productboard + Community tags on ADO #4144" reasoning the two sibling DISCUSSes used. JTBD differentiation from siblings is the substantive new content of this feature (`job-delivery-lead-spot-workflow-constraint` is materially distinct from both sibling jobs).

## Wave: DISCUSS / [REF] Outcome KPIs

| ID | Target | Scope | Measurement |
|---|---|---|---|
| `OUT-cumulative-state-time-adoption` | ≥30% of team / portfolio detail page sessions include a view of the Cumulative Time per State widget within 6 weeks of release (giving sibling 1's transitions table 6 weeks to populate) | per_instance | Backend counter on the new endpoint: increment per call from the Flow Metrics view; sample at week 6 against total Flow Metrics page-views. |
| `OUT-cumulative-state-time-leadership-share` | Within 8 weeks of release, ≥3 community / customer mentions of "we used the Cumulative Time per State chart in our retro / leadership review" (signal of the chart's adoption in its intended high-stakes contexts, not just casual browsing) | vendor_demo_only + community reports | Slack / forum / issue-tracker label `feature-use-cumulative-state-time`; sample at week 8. |
| `OUT-cumulative-state-time-constraint-naming` | Within 12 weeks of release, ≥10% of teams with the chart adoption signal also have at least one configuration change (e.g. WIP limit adjustment, blocked-indicator change, throughput-filter rule) within 30 days of viewing the chart — proxies the "chart leads to a process change" causal chain | per_instance | Cross-correlate the `cumulativeStateTime` endpoint hits with subsequent Team/Portfolio settings PUT events on the same team; lag of ≤30 days. |
| `OUT-cumulative-state-time-item-filter-usage` | Within 8 weeks of release, ≥15% of chart-viewing sessions use the item picker (US-05) at least once — validates the B2-absorption bet (D15) that the per-item/subset *distribution* lens is wanted; a near-zero rate is the signal that the dropped *chronology* lens may have been the actual need | per_instance | Backend counter on the `cumulativeStateTime/candidates` endpoint and on bar-data calls carrying a non-empty `itemIds`; sample at week 8 against total chart-viewing sessions. |

KPIs will be appended to `docs/product/kpi-contracts.yaml` at the DEVOPS handoff.

## Wave: DISCUSS / [REF] Definition of Ready — validation

| # | DoR item | Verdict | Evidence |
|---|---|---|---|
| 1 | Every story traces to a `job_id` | Pass | US-01, US-02, US-04 → `job-delivery-lead-spot-workflow-constraint`; US-05 → that job (subset lens) AND `job-po-deep-dive-item-state-time` (single-item deep-dive, absorbed from B2 per D15). US-03 withdrawn (D13). Both jobs in `docs/product/jobs.yaml`. |
| 2 | Persona named & scoped | Pass | `delivery-lead-rte` primary (`docs/product/personas/delivery-lead-rte.yaml`); `flow-coach` secondary (retro facilitation); `product-owner` secondary (NEW file `docs/product/personas/product-owner.yaml`, the absorbed-B2 outlier-deep-dive actor for US-05 single-item). |
| 3 | Elevator pitch per non-`@infrastructure` story | Pass | US-01, US-02, US-04, US-05 each have a Before/After/Decision triplet. After lines reference real entry points (`/teams/{teamId}` & `/portfolios/{portfolioId}` Flow Metrics, bar click for US-04, the item picker for US-05); sees-portions describe concrete observable output; decision-enabled lines name the decision. US-03 withdrawn (no pitch needed). |
| 4 | AC testable, no ambiguous outcomes | Pass | Quantified bar-height formula (D5 full-duration with D12 inclusion); adaptive-unit rule (D16) with the largest-bar selection criterion; explicit empty-state / zero-contributing-state / single-item / state-removed behaviour; quantified RAG-rule thresholds (40% / 60%) computed on the whole set (D18); explicit tooltip field list (US-01); drill-down panel column list, sort, keyboard/ARIA, sum-equals-bar-height check (US-04); picker search-keys, parent-expand, candidate=D12-included set (D17), RAG-unchanged-by-selection, single-item rendering, accessibility (US-05). |
| 5 | Out-of-scope explicit | Pass | 12 items listed (total/mean toggle, runtime full-vs-clip toggle, comparison view, per-item state CHRONOLOGY [B2's dropped lens], business-hours time, adaptive-unit override, cross-scope aggregation, configurable bar ordering, configurable RAG thresholds, transition-schema changes, backfill, blocked attribution, shared aggregation service with sibling F). |
| 6 | Outcome KPIs measurable with targets | Pass | 4 KPIs, each with numeric target, scope, and measurement method. `constraint-naming` tests the causal chain "chart → process change"; `item-filter-usage` validates the D15 B2-absorption bet (is the deep-dive distribution lens used, or was chronology the real need?). Drill-down click-through stays a secondary signal of `leadership-share`, not its own KPI. |
| 7 | Pre-requisites resolved | Pass (with sequencing note) | bug-5016 merged. Sibling 1 (`time-in-state-and-staleness`) is now merged & CI-green on main, so the DELIVER-time transitions-table blocker is satisfied. Sibling F (`aging-pace-percentiles`) shipped. Existing Flow Metrics category infrastructure confirmed present via code reality check. |
| 8 | Slice composition: each slice contains ≥1 user-visible story | Pass | Slice 01 → US-01 (team chart + adaptive units). Slice 02 → US-02 (portfolio parity; US-03 withdrawn so no tooltip-enrichment story). Slice 03 → US-04 (per-item drill-down). Slice 04 → US-05 (item picker + parent-expand, absorbs B2 distribution). No `@infrastructure`-only slices. |
| 9 | Handoff target identified | Pass | `nw-solution-architect` (DESIGN — **needs an amend pass**: picker component + candidate/itemIds endpoints, `formatDuration` util, US-03 removal, US-05 modal-vs-inline + drill-down-modal-vs-side-panel choices); `nw-platform-architect` (DEVOPS, `outcome-kpis` only). |

**DoR overall verdict: re-validated 2026-05-26 after the US-03/US-05/B2/units revision — PASSED (9/9 with evidence).**

## Wave: DISCUSS / [REF] Wave decisions summary

**Primary user need**: enable Delivery Leads / RTEs to identify the team's or portfolio's systemic workflow constraint from a single chart-glance, with evidence sufficient to defend the constraint claim in a leadership review or retrospective and to direct the next improvement-planning budget. Differentiated from siblings 1 (per-item triage badge) and F (per-item-on-aggregate-bands pace recognition): this feature answers the per-STATE-on-aggregate question that neither sibling addresses. **Secondary need (added 2026-05-26, absorbed from B2 per D15)**: let a user scope the same chart to a single item or a chosen subset (e.g. all stories under a feature) to see where THOSE items' time went — a Product-Owner outlier-deep-dive lens alongside the systemic view.

**Foundation investment**: zero new persistence; reuses `WorkItemStateTransition` (sibling 1) and existing `WorkItem.currentStateEnteredAt` (sibling 1). Three new endpoints per scope (bar data, per-state drill-down items, picker candidates), each accepting the window + optional `itemIds`; one new chart widget; one drill-down panel; one item picker; one `formatDuration` util; three metadata entries (`categoryMetadata`, `widgetInfoMetadata`, `ragRules`). Backwards-compatible throughout — no schema changes, no existing-endpoint changes.

**Walking skeleton scope**: slice 01 (US-01) — team scope only, the new bar-data endpoint, the new widget rendering in the Flow Metrics category, the adaptive display unit (D16), empty/zero-state edge cases handled; no portfolio scope, no drill-down, no picker. Proves the data-foundation → endpoint → widget path in one slice. Slice 02 adds US-02 (portfolio parity; US-03 withdrawn per D13). Slice 03 adds US-04 (per-item drill-down panel + per-state items endpoint). Slice 04 adds US-05 (item picker + parent-expand + candidate endpoint, `itemIds` wired through bar + drill-down endpoints) — the absorbed-B2 distribution lens.

**Feature type**: user-facing (Flow Metrics widget extension).

**Upstream changes**: **NONE for sibling 1 DESIGN**. D9 explicitly preserves `WorkItemStateTransition` schema as shipped by sibling 1 slice 01. **NONE for sibling F**. D10 flags an OPTIONAL DESIGN-time coordination opportunity (shared per-state aggregation service) but neither feature depends on the other to ship MVP-correct.

**Downstream coordination**: this is the third and final MVP-bundle feature. The MVP gate lifts when all three reach Done. Three-way DESIGN-time coordination on the per-state aggregation primitive is the only cross-feature design touchpoint identified.

## Wave: DISCUSS / [REF] Cross-MVP DESIGN coordination notes

The three Epic 4144 MVP-bundle features all consume `WorkItemStateTransition`. The DESIGN wave for the three should explicitly coordinate on the following:

1. **Shared per-state aggregation primitive (D10)**: sibling F computes per-state percentiles of age-at-state-exit for completed items; this feature computes per-state cumulative time across all items (completed + in-flight). Both walk the same transition data and group by state. A shared `IPerStateAggregationService` with two methods (`GetPerStatePercentiles(team, window)` and `GetPerStateCumulativeTime(team, window)`) would deduplicate the per-state loop and could share a materialised cache. NOT required for MVP correctness; flagged for DESIGN consideration.
2. **Materialised cache key strategy**: if a per-state cache materialises, the cache key must include (teamId-or-portfolioId, scope-type, startDate, endDate, type-filter-hash). Sibling 1 already touches the metrics cache; bug-5016 made it thread-safe; the new endpoints inherit that cache infrastructure.
3. **Date-range semantics are intentionally different across the bundle (sharper after D5 redirect)**: sibling F uses "history window" with COMPLETED items only (matches `cycleTimePercentiles` — the distribution is over historical age-at-state-exit of items that finished in the window). This feature uses "full-duration attribution with frame-based item selection" (D5 revised 2026-05-24; D12 inclusion rule) — items that touched the window during their life are included and their FULL state-durations are counted regardless of when those durations happened. DESIGN must NOT share a helper that conflates the two; the two endpoints should be named and documented to make the distinction unmistakable, since a sophisticated user will ask "why are the bar-chart numbers higher than the aging-chart bands suggest?" and the correct answer is "different question, different inclusion + attribution rule".
4. **In-flight contribution treatment**: this feature's D11 mandates in-flight contribution (their FULL current-state time, per revised D5); sibling F's per-state bands are derived from COMPLETED items only. DESIGN should ensure the two endpoints don't accidentally share a query helper that has different in-flight semantics for each — particularly important now that this feature uses full-duration attribution rather than the previously-locked clip-to-window math.
5. **Endpoint naming consistency**: sibling F's new endpoint is `/metrics/ageInStatePercentiles`; this feature's is `/metrics/cumulativeStateTime`. DESIGN should confirm both names are stable before either ships (renaming after either is consumed externally is expensive).

## Wave: DISCUSS / [REF] Changed Assumptions

Per the `Document Update (Back-Propagation)` contract in `nw-discuss/SKILL.md`, contradictions between DISCUSS findings and prior artifacts must be flagged. Unlike sibling F's DISCUSS (which corrected the README's "no bands exist today" premise), this feature's pre-DISCUSS code reality check **confirmed** the README's premise (no cumulative-time-per-state chart exists in Lighthouse today). The README does not require contradictory annotation, but for next-reader trust the README has been annotated with a quoted-block "verified" note pointing to this feature-delta — mirroring the pattern applied to `aging-pace-percentiles`'s README. The annotation is informational, not corrective: it documents that the gap claim was verified by code inspection, so a future reader does not have to repeat the inspection.

### Additions (informational, not contradictory)

**New persona**: `delivery-lead-rte` did not exist as a persona file before this DISCUSS run. The carry-over README referenced "Delivery Lead / RTE" as a persona candidate; this DISCUSS formalises it as a SSOT persona with file at `docs/product/personas/delivery-lead-rte.yaml`. Differentiated from `flow-coach` (per-item daily triage) and `delivery-forecaster` (forward-looking forecast honesty); no conflict with either.

**New job**: `job-delivery-lead-spot-workflow-constraint` did not exist as a SSOT job before this DISCUSS run. The carry-over README implied the job ("where does the team spend its time?") but didn't formalise it. This DISCUSS adds it to `docs/product/jobs.yaml` with full JTBD analysis (functional/emotional/social dimensions, four forces, opportunity score), explicitly differentiated from the two sibling jobs.

**New journey**: `docs/product/journeys/state-time-cumulative-view.yaml` created in this DISCUSS run with the full emotional-arc + steps + error-paths + design-decisions structure used by sibling F.

No DISCOVER document exists for Epic 4144 (per sibling 1's note); this DISCUSS extends the same community-validation reasoning, which has not been contradicted by any subsequent finding.

### Post-DISCUSS revision (2026-05-24) — D5 flipped, US-04 added

**Source**: user redirect after reviewing the DISCUSS output.

**Original D5 (pre-redirect)**: clip-to-window — bars showed only the time-in-state actually spent during the filter window; an item that entered Review 30 days before the window and exited 10 days in contributed 10 days to the Review bar. Rationale at the time: matched the "throughput in last quarter" mental model; avoided surprise-stretch from historical drag.

**Revised D5 (post-redirect, current)**: full-duration attribution with frame-based item selection. The window selects WHICH items count (per the new D12 inclusion rule); for each included item, the FULL state-durations contribute regardless of when they happened. Same item in the example now contributes its full 50 days. **User verbatim (2026-05-24)**: *"we pick the items that were relevant within the frame, but then look at the full time. so even if an item was closed on the 1st day of the window, if it was in progress 40 days before that, we should count the full time."*

**Rationale for the redirect**: clip-to-window under-counts the *real* cycle time of items relevant to the period and conceals the actual cost of slow states. For a leadership-review / retro audience the "real cycle pattern" framing is more actionable and harder to mislead with. The downside (bars become larger and sometimes much larger when items are stuck across window boundaries) is intentional — that's the signal the audience needs.

**New US-04 added**: per-item drill-down on bar click. Motivated by the corollary user requirement: with full-duration attribution, "which items drove this bar?" becomes the natural follow-up question. The drill-down panel lists contributing items with per-item day counts, sortable, accessible. Implementation choice (modal vs side panel) deferred to DESIGN. The drill-down also acts as the chart's "show your work" affordance — leadership can sanity-check the bar height by sum-of-rows.

**Items moved from out-of-scope to in-scope**: per-state drill-down (previously deferred to feature B2). Note: B2 (`work-item-state-history-view`) remains post-MVP for the inverse view (per-item → list of states); the two are complementary.

**Items moved from in-scope to out-of-scope**: runtime full-vs-clip toggle (added to out-of-scope as a deliberate "if telemetry demands" follow-up rather than a hidden assumption).

**New slice added**: slice-03 covers US-04 (per-item drill-down). Effort estimate: ~1–2 crafter days. Total feature effort grows from ~2 to ~3–4 crafter days. Slice ordering: 01 → 02 → 03 unchanged in dependency; all three remain within one MVP cycle.

**Cross-MVP impact of the redirect**: the date-range-semantics divergence between this feature and sibling F is now SHARPER (this feature: full-duration + frame-based inclusion; sibling F: completed-items-only with history window for the distribution). DESIGN coordination note 3 has been updated to reflect this — the two endpoints must NOT share a helper that conflates the rules.

**ADO follow-up flagged (not yet executed)**: ADO Story #5076 ("Cumulative time-per-state bar chart for filtered items") still describes US-01 well, so no rename needed. A new ADO Story for US-04 (per-item drill-down) should be added under Epic #4144 — flagged for the next /ado-sync invocation rather than auto-created.

### Post-DISCUSS revision (2026-05-26) — US-03 withdrawn, US-05 added, B2 absorbed, adaptive units

**Source**: user discussion after reviewing the DISCUSS+DESIGN output. Decisions D13–D18 added above; user verbatim quotes are recorded inline in each decision.

**US-03 withdrawn (D13)**: the dedicated tooltip "included items" line is removed; the per-state drill-down (US-04) is the stronger "which items?" affordance. Completed/ongoing counts stay in US-01's tooltip; the inclusion-rule explanation moves to `widgetInfoMetadata.ts`. ID retained (not renumbered) to keep ADO #5076 and DESIGN references anchored.

**US-05 added (D14)**: an in-chart multi-select item picker (search by Reference ID or Name; parent-expand to "select all children of a feature/epic") scopes the bars from the systemic all-items default to a single item or a chosen subset. The transpose of US-04.

**Feature B2 absorbed (D15)**: `work-item-state-history-view` is retired from Epic 4144. Its *distribution* lens (per-item time-per-state) lands here via US-05 single-item selection; its *chronology* lens (ordered transition timeline, re-entries with dates) is **dropped** for MVP, follow-up only if asked. Adds the `product-owner` persona + `job-po-deep-dive-item-state-time` job.

**Adaptive units (D16)**: bar height computed at full calendar-time precision; displayed in one adaptive unit per render (minutes → hours → days → weeks), uniform across bars. Replaces the fixed-days display. Wall-clock/calendar time (no business-hours).

**Two semantics nailed down (D17, D18)**: the date window gates the picker's candidate set (an item not touched in the window isn't selectable); the RAG status always reflects the whole in-scope set and never changes with the picker selection (same as hiding a work-item-type in the cycle-time chart).

**New endpoints**: `cumulativeStateTime/candidates` per scope (picker list); optional `itemIds` query param added to the bar-data and drill-down endpoints. No schema change; still Type A additive.

**New slice added**: slice-04 covers US-05 (item picker + parent-expand). slice-02 loses its US-03 tooltip-enrichment half (now portfolio parity only). Feature effort grows by ~1–2 crafter days; slice ordering 01 → 02 → 03 → 04 within one MVP cycle.

**DESIGN amend — DONE (2026-05-26)**: the DESIGN sections below were STALE after this revision (they referenced US-03 in DDD-6, lacked the US-05 picker + `candidates`/`itemIds` endpoints + `formatDuration` util, and assumed fixed-days display). The DESIGN amend pass reconciled them on 2026-05-26 (Morgan, PROPOSE): DDD-6 reframed to US-01; DDD-18..24 added; new ADR-028; SSOT brief + C4 diagrams updated. The STALE banner is lifted (see the RECONCILED banner at the DDD list). Ready for DISTILL.

**ADO sync (executed 2026-05-26)**: Story #5082 (US-03 tooltip breakdown) → **Removed**; new Story **#5103** ("Item picker: scope cumulative state-time chart to selected items", US-05) created and parented under Epic #4144. Existing #5076 (US-01), #5081 (US-02), #5083 (US-04) unchanged — titles still accurate. No B2 (`work-item-state-history-view`) ADO item existed (it was catalog-level "TBD"), so B2-absorption needed no removal.

## Wave: DESIGN / [REF] DDD list

> **RECONCILED (2026-05-26, amend pass — Morgan, interaction mode PROPOSE)** — the prior ⚠ STALE banner is lifted. These DESIGN sections now reflect the D13–D18 revision: DDD-6 reframed from US-03 to US-01 (counts still computed); DDD-18..DDD-24 added for the US-05 item picker, the `candidates` endpoint, the `itemIds` subset filter, the adaptive-unit `formatDuration` util, the D18 RAG-ignores-`itemIds` invariant, the D17 candidate=windowed-set rule, and the B2-absorption (single-item distribution rendering + PO persona/job). The still-valid math DDDs (DDD-1..DDD-5, DDD-7..DDD-10) are preserved unchanged. New ADR-028 carries the amend decisions; ADR-022/023/024/025 are unchanged and still authoritative for the math, drill-down, no-shared-service, and chart-widget decisions.

| ID | Decision | Verdict | One-line rationale |
|---|---|---|---|
| DDD-1 | Item-membership rule (D12 concretised): UNION of (a) any `(stateEnter, stateExit)` interval intersects window AND (b) currently in-flight at `windowEnd` (`StateCategory != Done AND CurrentStateEnteredAt <= windowEnd`) | Locked | Implements DISCUSS D12 verbatim; UNION is required because (b)-only items (newly-started, no completed transitions) must still contribute via in-flight attribution; ADR-022 §1 |
| DDD-2 | Per-visit duration formula: `visitDuration = exitTransition.TransitionedAt - entryTransition.TransitionedAt` (FULL, unclipped). Visit-level (not item-level) primitive; item-level totals derive from summing visits | Locked | Implements DISCUSS D5 verbatim; visit-level is the natural intermediate for the segment-split; re-entries through a state contribute multiple observations; ADR-022 §2 |
| DDD-3 | In-flight duration formula: `inFlightDuration = nowSnapshot - W.CurrentStateEnteredAt` (FULL, unclipped). Single `now` snapshot per request for determinism (passed as a parameter to the helper) | Locked | Implements DISCUSS D11 verbatim; determinism is non-negotiable for the US-04 sum-equals-bar-height invariant; ADR-022 §3 |
| DDD-4 | Segment-split rule (D6): `completedContribution[S] = Σ visitDuration over completed visits through S`; `ongoingContribution[S] = Σ inFlightDuration over included items currently in S`; `totalDays[S] = completed + ongoing` | Locked | Implements DISCUSS D6 verbatim; the two segments are disjoint by construction; ADR-022 §4 |
| DDD-5 | Per-item drill-down attribution (US-04): `daysContributed(W, S) = Σ visitDuration through S + (inFlightDuration if W.State == S AND in-flight else 0)`; drill-down endpoint sum equals bar endpoint's `totalDays[S]` within ±0.1d tolerance by construction | Locked | Implements US-04 AC line 4 verbatim; sum-equals-bar-height invariant holds by formula identity, not coincidence; ADR-022 §5 |
| DDD-6 | Counts for US-01 tooltip (reframed from US-03, withdrawn per D13): `completedItemCount[S] = distinct included items with ≥1 completed visit through S`; `ongoingItemCount[S] = included items currently in S`; `itemCount[S] = distinct contributing items` (note: completed + ongoing may exceed itemCount because re-entered items can be in both buckets). The COUNTS still ship in US-01's tooltip (already in the bar-endpoint payload, ADR-022 §6). The inclusion+attribution EXPLANATION (D5/D12 plain-language) moves OUT of the tooltip into `widgetInfoMetadata.ts` learn-more text per D13 | Locked | Counts computation is unchanged from the original ADR-022 §6; D13 only moved the standalone explanation line to widget-info and dropped US-03. Tooltip wording for the non-additive case ("A closed in window AND B still in flight") is retained in US-01's tooltip; ADR-022 §6 + ADR-028 §1 |
| DDD-7 | Mean + median per item: `meanDays = Σ perItemContributions / count`; `medianDays = PercentileCalculator.CalculatePercentile(values, 50)` — reuses sibling F's percentile primitive for algorithmic parity | Locked | Same convention as `cycleTimePercentiles` median computation; ADR-022 §7 |
| DDD-8 | Workflow state ordering: returned in the team's / portfolio's existing `WorkflowStates` order (same source the chart's X-axis uses for sibling F's `WorkItemAgingChart`). Done-category contributions aggregate into the team's `doneStates.First()` bar | Locked | DISCUSS D3 mandates workflow-order; consistency with `WorkItemAgingChart` X-axis preserves the user's mental model; ADR-022 §8 |
| DDD-9 | Empty/zero-state behaviour: empty `states: []` when no items match (FE renders empty-state); zero-contributing state still emitted with `totalDays: 0` for placeholder rendering; unknown state name in drill-down endpoint returns empty `items: []` (graceful) | Locked | Implements DISCUSS US-01 AC lines 5 + 6 + US-04 empty case; no backend threshold suppression; ADR-022 §9 |
| DDD-10 | Caching policy: cache keys `CumulativeStateTime_{startDate:yyyy-MM-dd}_{endDate:yyyy-MM-dd}` and `CumulativeStateTime_Items_{state}_{startDate}_{endDate}` — distinct from sibling F's `AgeInStatePercentiles_…` namespace. Inherits existing post-sync invalidation hook | Locked | Zero new cache infrastructure; namespace divergence prevents semantic conflation; ADR-022 §10 |
| DDD-11 | Drill-down endpoint shape: SEPARATE endpoint `/cumulativeStateTime/items?state=X&startDate&endDate` per scope. NOT an `?expand=items` parameter on the bar endpoint | Locked | Keeps bar payload slim for the 80%-glance case; per-state cache granularity; type cleanliness; ADR-023 §1 |
| DDD-12 | Drill-down UI primitive: MUI `Dialog` modal (component `CumulativeStateTimeDrillDownDialog.tsx`), following the `WorkItemsDialog` precedent. Zero `Drawer` files exist in the codebase; `Dialog` is the universal "table-from-chart-click" pattern | Locked | In-codebase consistency; reuses `DataGridBase` for table mechanics; ADR-023 §2 |
| DDD-13 | Chart widget: NEW `CumulativeStateTimeChart.tsx` component using MUI-X `<BarChart>` with horizontal layout, stacked completed/ongoing series, SVG `<pattern>`-based hatching for the ongoing segment. Does NOT extend `WorkItemAgingChart` | Locked | Different data shape, different question; MUI-X `BarChart` natively supports stacked bars; ADR-025 §1+§2 |
| DDD-14 | Widget registration: single entry `stateTimeCumulative` in `categoryMetadata.ts` (`flow-metrics`, size `large`, no `ownerFilter`), `widgetInfoMetadata.ts`, and `ragRules.ts` (new `computeCumulativeStateTimeRag` with 40%/60% thresholds). Dispatched by `BaseMetricsView.tsx` to both scopes via the existing `widgetKey`-based dispatch | Locked | Established codebase pattern (cf. `cycleScatter`, `aging`, `throughput` entries with no `ownerFilter`); ADR-025 §4 |
| DDD-15 | ADR-018 + ADR-021 disposition: UPHELD (Path A-prime). NO shared `IPerStateAggregationService`. Per-state cumulative computation lives as two NEW `protected` helpers inside `BaseMetricsService` (`ComputeCumulativeStateTime`, `ComputeCumulativeStateTimeItems`), parallel to sibling F's `ComputeAgeInStatePercentiles`. Three-way cross-MVP convergence | Locked | ADR-024 — three independent DESIGN re-litigations reach the same conclusion; conflation risk is concrete after ADR-022 + ADR-019; cache-sharing win is zero at MVP scale; supersession cost across three feature boundaries is high |
| DDD-16 | Endpoint route shape: `GET /api/teams/{teamId:int}/metrics/cumulativeStateTime` and `/cumulativeStateTime/items?state=X` (and portfolio equivalents). Validation: `startDate.Date <= endDate.Date` (HTTP 400 with `StartDateMustBeBeforeEndDateErrorMessage`); drill-down endpoint additionally requires non-empty `state` (HTTP 400 if missing); unknown state returns HTTP 200 with empty `items: []` | Locked | Mirrors `cycleTimePercentiles` validation pattern; graceful handling of placeholder-bar clicks; DESIGN mechanical, no ADR |
| DDD-17 | Auth: existing `[RbacGuard(TeamRead, ScopeIdRouteKey="teamId")]` / `[RbacGuard(PortfolioRead, ScopeIdRouteKey="portfolioId")]` class-level guards inherited by all SIX new endpoints (bar, drill-down items, picker candidates × team + portfolio); no new auth surface | Locked | Per CLAUDE.md "no component fetches RBAC summaries directly"; inherits established controller-level guard pattern; DESIGN mechanical |
| DDD-18 | In-chart item picker (US-05): NEW `CumulativeStateTimeItemPicker.tsx` built on MUI `Autocomplete` with `multiple` + `Chip` selected-tags (the established item/feature-selection pattern in `ManualForecaster.tsx`), placed as a chart-toolbar control above the bars. Search matches Reference ID OR Name only (D14); parent-expand surfaces as an inline "Select all N children" action on candidate rows whose `parentReferenceId` is itself a matched candidate. Emits the selected item-id set; the chart passes it as `itemIds` to the bar + drill-down fetches. Default cleared = systemic all-items view (D14). Keyboard + SR accessible per US-05 AC | Locked | Reuses the in-codebase `Autocomplete`+`Chip` multi-select idiom (zero new dependency); chart-toolbar placement keeps the picker visually bound to the chart it scopes; parent-expand-as-row-action avoids a separate tree UI; ADR-028 §2 |
| DDD-19 | Picker candidate set (D17): NEW `candidates` endpoint per scope returns exactly the D12-included items for the window + ambient filters (`{ workItemId, referenceId, title, workItemType, parentReferenceId? }`). An item not touched in the window is not in the candidate list and therefore not selectable. Candidate resolution REUSES the same D12-inclusion query the bar endpoint already runs (DDD-1) — the picker is fed by the same membership rule, never a wider set | Locked | D17 verbatim ("the date window selects which items are even available"); reusing the D12 query guarantees picker candidates and bar contributors are the same population; `parentReferenceId` reads the existing vendor-neutral `WorkItemBase.ParentReferenceId` (populated by all connectors) so parent-expand works across Jira/ADO/Linear/CSV with no schema change; ADR-028 §3 |
| DDD-20 | `itemIds` subset filter (US-05): the bar and drill-down endpoints accept an OPTIONAL `itemIds` query param (DDD-22 transport). Absent ⇒ all D12-included items (systemic view). Present ⇒ the SAME D12-inclusion query runs, then the result is INTERSECTED with `itemIds` before the per-state walk — the selection narrows the population but never bypasses inclusion (D17). Full-duration attribution (D5) and segment-split (D6) apply unchanged to the narrowed set; the adaptive unit (DDD-21) re-chooses for the smaller magnitude | Locked | `itemIds` is a post-inclusion intersection, never a pre-inclusion bypass — guarantees a selected-but-out-of-window id cannot smuggle an excluded item in; reuses the entire existing per-state walk; ADR-028 §3 |
| DDD-21 | Adaptive uniform display unit (D16): NEW `formatDuration` util (`Lighthouse.Frontend/src/utils/date/formatDuration.ts`) chooses ONE display unit (minutes → hours → days → weeks) per chart render from the LARGEST bar's magnitude and formats every bar uniformly in that unit. Backend contract stays a single canonical numeric — `totalDays` (double, full calendar precision); the FE owns unit selection. The util is pure (`(maxMagnitudeDays: number) => { unit, format(valueDays) }` shape — exact signature a GREEN detail) and reused for bar labels, the value axis label, and the tooltip's primary value (tooltip may also show a finer secondary value) | Locked | No existing adaptive/laddering formatter in the codebase (`age.ts` returns day-counts; `chartAxisUtils.ts` formats raw numbers) — genuinely NEW; keeping the contract numeric in days and adapting on the FE avoids a backend unit decision and keeps cross-endpoint comparability; ADR-028 §4 |
| DDD-22 | `itemIds` transport: REPEATED query params (`?itemIds=12&itemIds=87&…`), bound as `[FromQuery] int[]? itemIds` (nullable to avoid the required-by-default 400 — see ci-learnings 2026-05-16). Endpoints STAY GET (cacheable, REST-consistent with the existing metrics endpoints). Practical selection sizes (a feature's children, a handful of items, or one item) keep the URL well under the ~2 KB safe limit; the parent-expand case selects child ids, not a giant flat list. If a future bulk-select feature blows the URL budget, switching that endpoint to POST is a documented follow-up | Locked | Repeated-param binding is the idiomatic ASP.NET array-query shape and preserves GET cacheability + the uniform `cycleTimePercentiles`-style controller pattern; comma-join was rejected (manual parsing, no model-binder support); POST was rejected for MVP (breaks GET caching + REST consistency for a payload that is small in practice); ADR-028 §5 |
| DDD-23 | RAG ignores `itemIds` (D18): `computeCumulativeStateTimeRag` runs on the WHOLE in-scope set and is computed from a bar-endpoint response fetched WITHOUT `itemIds`. The FE keeps the systemic (no-selection) bar data as the RAG source even while displaying a picker-narrowed set; the narrowed fetch drives ONLY the rendered bars, never the RAG. Analogy locked by the user: hiding a work-item-type in the cycle-time chart does not change its RAG | Locked | D18 verbatim; the picker is a view-level lens, not a recompute of the systemic health signal; the FE already holds the systemic response in `useMetricsData` ctx, so the RAG simply reads that and ignores the selection; ADR-028 §6 |
| DDD-24 | B2 absorption (D15): a single-item picker selection renders that one item's per-state DISTRIBUTION (one bar per state it visited) using the IDENTICAL bar arithmetic + segments + adaptive unit — no new rendering path, just `itemIds=[oneId]`. The CHRONOLOGY lens (ordered transition timeline, re-entry sequence) is explicitly NOT built (D15 out-of-scope). Adds secondary persona `product-owner` + job `job-po-deep-dive-item-state-time` to the feature's traceability | Locked | Distribution lens falls out of US-05 for free (single-item is just the n=1 subset case); chronology is deliberately dropped per D15; no extra component or endpoint needed for the PO deep-dive; ADR-028 §7 |

## Wave: DESIGN / [REF] Component decomposition

| Component | File | Change Type | Change Summary |
|---|---|---|---|
| `CumulativeStateTimeDto` + `CumulativeStateTimeStateRowDto` (NEW DTOs) | `Lighthouse.Backend/Lighthouse.Backend/API/DTO/CumulativeStateTimeDto.cs` (one file, two records) | NEW | Bar-endpoint response. `record CumulativeStateTimeDto(IReadOnlyList<CumulativeStateTimeStateRowDto> States)`. `record CumulativeStateTimeStateRowDto(string State, int WorkflowOrder, double TotalDays, double CompletedContributionDays, double OngoingContributionDays, int ItemCount, int CompletedItemCount, int OngoingItemCount, double MeanDays, double MedianDays)`. |
| `CumulativeStateTimeItemsDto` + `CumulativeStateTimeItemRowDto` (NEW DTOs) | `Lighthouse.Backend/Lighthouse.Backend/API/DTO/CumulativeStateTimeItemsDto.cs` | NEW | Drill-down response. `record CumulativeStateTimeItemsDto(string State, IReadOnlyList<CumulativeStateTimeItemRowDto> Items)`. `record CumulativeStateTimeItemRowDto(string WorkItemId, string Title, string WorkItemType, string CurrentState, double DaysContributed)`. |
| `CumulativeStateTimeCandidatesDto` + `CumulativeStateTimeCandidateRowDto` (NEW DTOs, US-05) | `Lighthouse.Backend/Lighthouse.Backend/API/DTO/CumulativeStateTimeCandidatesDto.cs` | NEW | Picker candidate response (DDD-19). `record CumulativeStateTimeCandidatesDto(IReadOnlyList<CumulativeStateTimeCandidateRowDto> Items)`. `record CumulativeStateTimeCandidateRowDto(int WorkItemId, string ReferenceId, string Title, string WorkItemType, string? ParentReferenceId)`. `ReferenceId` + `Title` are the search keys (D14); `ParentReferenceId` (read from the existing vendor-neutral `WorkItemBase.ParentReferenceId`) drives parent-expand. `WorkItemId` is the numeric DB id passed back as `itemIds`. |
| `ITeamMetricsService` | `Lighthouse.Backend/Lighthouse.Backend/Services/Interfaces/ITeamMetricsService.cs` | EXTEND | Add three methods: `CumulativeStateTimeDto GetCumulativeStateTimeForTeam(Team team, DateTime startDate, DateTime endDate, IReadOnlyList<int>? itemIds)`; `CumulativeStateTimeItemsDto GetCumulativeStateTimeItemsForTeam(Team team, string state, DateTime startDate, DateTime endDate, IReadOnlyList<int>? itemIds)`; `CumulativeStateTimeCandidatesDto GetCumulativeStateTimeCandidatesForTeam(Team team, DateTime startDate, DateTime endDate)`. The `itemIds` param is the US-05 subset filter (DDD-20); `null`/empty = systemic all-items view. |
| `IPortfolioMetricsService` | `Lighthouse.Backend/Lighthouse.Backend/Services/Interfaces/IPortfolioMetricsService.cs` | EXTEND | Same shape as Team — three new methods for the portfolio scope (bar+`itemIds`, items+`itemIds`, candidates). |
| `BaseMetricsService` | `Lighthouse.Backend/Lighthouse.Backend/Services/Implementation/BaseMetricsService.cs` | EXTEND | Add two `protected` helpers: `ComputeCumulativeStateTime(IEnumerable<WorkItem> includedItems, Func<int, IEnumerable<WorkItemStateTransition>> getTransitionsForItem, IEnumerable<string> workflowStatesInOrder, DateTime nowSnapshot)` returning `CumulativeStateTimeDto`; `ComputeCumulativeStateTimeItems(IEnumerable<WorkItem> includedItems, Func<int, IEnumerable<WorkItemStateTransition>> getTransitionsForItem, string selectedState, DateTime nowSnapshot)` returning `CumulativeStateTimeItemsDto`. Per ADR-022 algorithm. INTRA-INHERITANCE only (ADR-024). The `itemIds` intersection (DDD-20) and the candidate projection (DDD-19) live in the derived `Team`/`Portfolio` services (they own the D12 query); the helpers stay subset-agnostic and receive the already-narrowed `includedItems`. |
| `TeamMetricsService` | `Lighthouse.Backend/Lighthouse.Backend/Services/Implementation/TeamMetricsService.cs` | EXTEND | Implement `GetCumulativeStateTimeForTeam` by: (1) resolve D12 candidate items via `workItemRepository.GetAllByPredicate` + transition-intersection check; (2) if `itemIds` is non-empty, INTERSECT the candidate set with it (DDD-20 — post-inclusion narrowing, never a bypass); (3) build the transition-fetcher delegate `id => transitionRepo.GetAllByPredicate(t => t.WorkItemId == id)` (or an EF-friendly batch shape — software-crafter at GREEN); (4) call `ComputeCumulativeStateTime(...)`; (5) wrap in `GetFromCacheIfExists(team, cacheKey, () => …, logger)` where `cacheKey` incorporates the `itemIds` selection (DDD-10 + DDD-20: `CumulativeStateTime_{startDate:yyyy-MM-dd}_{endDate:yyyy-MM-dd}` for the systemic case; a selection-hash suffix when `itemIds` is present so the systemic and narrowed responses cache independently). Implement `GetCumulativeStateTimeItemsForTeam` similarly (cache key `CumulativeStateTime_Items_{state}_{startDate}_{endDate}` + selection suffix). Implement `GetCumulativeStateTimeCandidatesForTeam` by running the SAME D12 query and projecting each item to `CumulativeStateTimeCandidateRowDto` (DDD-19); cache key `CumulativeStateTime_Candidates_{startDate}_{endDate}`. Workflow state order from the team's existing `WorkflowStates`. |
| `PortfolioMetricsService` | `Lighthouse.Backend/Lighthouse.Backend/Services/Implementation/PortfolioMetricsService.cs` | EXTEND | Same pattern as Team, scoped to the portfolio's items (bar+`itemIds`, items+`itemIds`, candidates). |
| `TeamMetricsController` | `Lighthouse.Backend/Lighthouse.Backend/API/TeamMetricsController.cs` | EXTEND | Add `[HttpGet("cumulativeStateTime")] ActionResult<CumulativeStateTimeDto> GetCumulativeStateTimeForTeam(int teamId, [FromQuery] DateTime startDate, [FromQuery] DateTime endDate, [FromQuery] int[]? itemIds)` with validation mirroring `cycleTimePercentiles` (HTTP 400 on inverted dates, `LogDateBoundaries`, `GetEntityByIdAnExecuteAction`). `itemIds` is `[FromQuery] int[]?` — NULLABLE to avoid the model-binder required-by-default 400 (ci-learnings 2026-05-16); absent ⇒ systemic view. Add `[HttpGet("cumulativeStateTime/items")] ActionResult<CumulativeStateTimeItemsDto> GetCumulativeStateTimeItemsForTeam(int teamId, [FromQuery] string state, [FromQuery] DateTime startDate, [FromQuery] DateTime endDate, [FromQuery] int[]? itemIds)` with additional `state` presence validation (HTTP 400 on empty/missing). Add `[HttpGet("cumulativeStateTime/candidates")] ActionResult<CumulativeStateTimeCandidatesDto> GetCumulativeStateTimeCandidatesForTeam(int teamId, [FromQuery] DateTime startDate, [FromQuery] DateTime endDate)` (no `state`, no `itemIds` — feeds the picker per DDD-19). |
| `PortfolioMetricsController` | `Lighthouse.Backend/Lighthouse.Backend/API/PortfolioMetricsController.cs` | EXTEND | Mirror the three endpoints for portfolio scope. |
| `WorkItem` model | `Lighthouse.Backend/Lighthouse.Backend/Models/WorkItem.cs` (existing) | NO CHANGE | DISCUSS D9 held — no schema additions. The D12 inclusion-rule reads existing fields: `Id`, `TeamId`, `State`, `StateCategory`, `CurrentStateEnteredAt` (shipped by sibling 1 ADR-016), `Title`, `Type` (existing). Per-item drill-down rows project `workItemId = $"{team.WorkTrackingSystemConfig.PrefixOrEquivalent}-{wi.ReferenceId}"` or equivalent — software-crafter selects at GREEN to match the existing `WorkItemDto` projection. |
| `MetricsService` (TS) + `IMetricsService` | `Lighthouse.Frontend/src/services/Api/MetricsService.ts` | EXTEND | Add SIX methods: `getCumulativeStateTimeForTeam(teamId, startDate, endDate, itemIds?: number[]): Promise<ICumulativeStateTimeResponse>`; `getCumulativeStateTimeForPortfolio(portfolioId, startDate, endDate, itemIds?)`; `getCumulativeStateTimeItemsForTeam(teamId, state, startDate, endDate, itemIds?): Promise<ICumulativeStateTimeItemsResponse>`; `getCumulativeStateTimeItemsForPortfolio(portfolioId, state, startDate, endDate, itemIds?)`; `getCumulativeStateTimeCandidatesForTeam(teamId, startDate, endDate): Promise<ICumulativeStateTimeCandidatesResponse>`; `getCumulativeStateTimeCandidatesForPortfolio(portfolioId, startDate, endDate)`. The four bar/items methods mirror `getCycleTimePercentiles` HTTP shape (line 247) and append `itemIds` as REPEATED query params when present (DDD-22). Added to the `IMetricsService` interface. |
| `useMetricsData` (hook) | `Lighthouse.Frontend/src/hooks/useMetricsData.ts` | EXTEND | Add `cumulativeStateTime: ICumulativeStateTimeResponse | null` (initial `null`) to the returned ctx — this is the SYSTEMIC (no-`itemIds`) bar data, the RAG source per DDD-23. In the same `useEffect` block that today fetches `cycleTimePercentiles`, add a parallel call to `metricsService.getCumulativeStateTime…(entity.id, startDate, endDate)` (NO `itemIds` — the hook always fetches the whole in-scope set). The picker-narrowed fetch and the drill-down items fetch are NOT in this hook — both are lazy and live in the chart-parent (BaseMetricsView). Candidate fetch is also chart-parent-owned (lazy on picker open). |
| `BaseMetricsView` | `Lighthouse.Frontend/src/pages/Common/MetricsView/BaseMetricsView.tsx` | EXTEND | Add a dispatch entry for `widgetKey === "stateTimeCumulative"`. Hold local state: selected `itemIds` (from the picker), the narrowed bar response (when a selection is active), the candidate list (lazy), and the drill-down dialog (selected state + resolved items + open flag). Render `<CumulativeStateTimeChart states={displayedStates} isLoading={…} onBarClick={handleBarClick} picker={<CumulativeStateTimeItemPicker …/>} />` where `displayedStates` = the narrowed response when `itemIds` is non-empty, else `ctx.cumulativeStateTime?.states ?? []`. RAG is ALWAYS computed from `ctx.cumulativeStateTime` (the systemic set, DDD-23) regardless of selection. `handleBarClick(stateName)` fetches `getCumulativeStateTimeItems…(…, itemIds)` (passing the active selection per US-05 AC) and opens `<CumulativeStateTimeDrillDownDialog>`. On picker change, refetch the bar data with the new `itemIds`. |
| `CumulativeStateTimeChart` | `Lighthouse.Frontend/src/components/Common/Charts/CumulativeStateTimeChart.tsx` | EXTEND (NEW component, picker-integrated) | MUI-X `<BarChart>` with `layout="horizontal"` (or vertical — software-crafter picks at GREEN), stacked completed/ongoing series, SVG `<defs><pattern id="cumulativeStateTimeOngoingHatch" .../></defs>` for hatching. Tooltip via `<ChartsTooltip>` custom slot renders the 10 fields PLUS the completed/ongoing COUNTS (US-01) — the US-03 standalone "included items" explanation line is REMOVED (D13; the explanation now lives in `widgetInfoMetadata.ts`). Bar labels, value axis, and tooltip primary value all formatted via `formatDuration` with one ADAPTIVE unit chosen from the largest bar (DDD-21). Accepts a `picker` render-slot (the `CumulativeStateTimeItemPicker`) placed in a chart toolbar above the bars. Bar `onClick` fires `props.onBarClick(stateName)`. Empty case + zero-contributing-state placeholder per AC; single-item selection renders as the n=1 distribution (DDD-24). |
| `CumulativeStateTimeItemPicker` | `Lighthouse.Frontend/src/components/Common/Charts/CumulativeStateTimeItemPicker.tsx` | NEW (US-05) | MUI `Autocomplete` (`multiple`, `Chip` selected-tags) following the `ManualForecaster.tsx` item-selection idiom. Props: `candidates: ICumulativeStateTimeCandidateRow[]`, `selectedItemIds: number[]`, `onSelectionChange: (ids: number[]) => void`, `isLoading`. Search filter matches `referenceId` OR `title` only (D14) via a custom `filterOptions`. Parent-expand (D14): a candidate whose `referenceId` is itself referenced by other candidates' `parentReferenceId` shows an inline "Select all N children" affordance (button/menu-item) adding those child ids in one action. Default (no selection) = systemic view. Keyboard + SR accessible (Autocomplete supplies combobox ARIA; the parent-expand action is a focusable, labelled control). Empty candidate set → disabled/empty state consistent with the chart's own empty state. |
| `formatDuration` util | `Lighthouse.Frontend/src/utils/date/formatDuration.ts` | NEW (D16/DDD-21) | Pure util that picks ONE display unit (minutes → hours → days → weeks) from a max-magnitude (in days) and formats values uniformly in that unit. Shape (exact signature a GREEN detail): `chooseDurationUnit(maxDays: number): DurationUnit` + `formatDuration(valueDays: number, unit: DurationUnit): string`. No existing util does this (`age.ts` returns day-counts; `chartAxisUtils.ts` formats raw numbers). Co-located with the existing `date/age.ts`. |
| `CumulativeStateTimeDrillDownDialog` | `Lighthouse.Frontend/src/components/Common/Charts/CumulativeStateTimeDrillDownDialog.tsx` | NEW | MUI `Dialog` + `DialogTitle` + `DialogContent` wrapping `DataGridBase` with columns (Work Item ID linkable, Title, Type, Current State, Days Contributed). Default sort: `daysContributed` desc. ARIA `role="dialog"`, focus trap, Escape closes, outside-click closes. Composes with US-05: when a picker selection is active, the parent passes `itemIds` to the items fetch so the panel lists only selected contributors (US-05 AC). Empty case: "No items contributed to this state in the selected window." per US-04 AC. |
| `ICumulativeStateTimeStateRow` + `ICumulativeStateTimeResponse` (TS models) | `Lighthouse.Frontend/src/models/Metrics/CumulativeStateTime.ts` | NEW | Mirror backend DTOs. `interface ICumulativeStateTimeStateRow { state, workflowOrder, totalDays, completedContributionDays, ongoingContributionDays, itemCount, completedItemCount, ongoingItemCount, meanDays, medianDays }`. `interface ICumulativeStateTimeResponse { states: ICumulativeStateTimeStateRow[]; }`. |
| `ICumulativeStateTimeItemRow` + `ICumulativeStateTimeItemsResponse` (TS models) | `Lighthouse.Frontend/src/models/Metrics/CumulativeStateTimeItems.ts` | NEW | `interface ICumulativeStateTimeItemRow { workItemId: string; title: string; workItemType: string; currentState: string; daysContributed: number; }`. `interface ICumulativeStateTimeItemsResponse { state: string; items: ICumulativeStateTimeItemRow[]; }`. |
| `ICumulativeStateTimeCandidateRow` + `ICumulativeStateTimeCandidatesResponse` (TS models, US-05) | `Lighthouse.Frontend/src/models/Metrics/CumulativeStateTimeCandidates.ts` | NEW | `interface ICumulativeStateTimeCandidateRow { workItemId: number; referenceId: string; title: string; workItemType: string; parentReferenceId?: string; }`. `interface ICumulativeStateTimeCandidatesResponse { items: ICumulativeStateTimeCandidateRow[]; }`. |
| `categoryMetadata.ts` | `Lighthouse.Frontend/src/pages/Common/MetricsView/categoryMetadata.ts` | EXTEND | Add `{ widgetKey: "stateTimeCumulative", size: "large" }` to the `flow-metrics` category list. No `ownerFilter` (renders in both team and portfolio scopes). Add `stateTimeCumulative: "none"` to `trendPolicies` map. |
| `widgetInfoMetadata.ts` | `Lighthouse.Frontend/src/pages/Common/MetricsView/widgetInfoMetadata.ts` | EXTEND | Add `stateTimeCumulative` entry with title `"Cumulative Time per State"`, description, learn-more URL, and RAG status guidance keyed on the three RAG values returned by `computeCumulativeStateTimeRag`. |
| `ragRules.ts` | `Lighthouse.Frontend/src/pages/Common/MetricsView/ragRules.ts` | EXTEND | Add `computeCumulativeStateTimeRag(states: readonly ICumulativeStateTimeStateRow[], terms: RagTerms): RagResult` returning `green` (no state ≥ 40% of total), `amber` (one state 40-60%), `red` (one state > 60%). Empty-states case returns `red` with filter-adjustment tip. |
| Vitest tests — `CumulativeStateTimeChart` | `Lighthouse.Frontend/src/components/Common/Charts/CumulativeStateTimeChart.test.tsx` | NEW | (a) Renders one bar per state in workflow order; (b) bar heights match completedContributionDays + ongoingContributionDays; (c) tooltip on hover contains all 10 fields + the completed/ongoing COUNTS (US-01) — asserts there is NO standalone US-03 "included items" explanation line (D13); (d) hatching pattern element present in DOM; (e) bar click fires onBarClick with state name; (f) empty case renders empty-state message; (g) zero-contributing state renders labelled placeholder; (h) bar labels/axis use the adaptive unit chosen from the largest bar (sub-day single-item magnitude renders in hours/minutes, not `0.x d`) (DDD-21); (i) renders the `picker` slot. |
| Vitest tests — `CumulativeStateTimeItemPicker` | `Lighthouse.Frontend/src/components/Common/Charts/CumulativeStateTimeItemPicker.test.tsx` | NEW (US-05) | (a) Lists candidates; (b) search filters by Reference ID; (c) search filters by Name; (d) search does NOT match other attributes (negative); (e) selecting items calls `onSelectionChange` with the right ids; (f) parent-expand "Select all N children" adds all child ids in one action; (g) cleared selection = empty (systemic) state; (h) keyboard: open/type/multi-select/parent-expand reachable; (i) ARIA combobox + labelled controls; (j) empty candidate set renders disabled/empty state. |
| Vitest tests — `formatDuration` util | `Lighthouse.Frontend/src/utils/date/formatDuration.test.ts` | NEW (DDD-21) | (a) Largest-bar magnitude drives the unit (minutes → hours → days → weeks); (b) chosen unit is uniform across values; (c) sub-day magnitude renders in hours/minutes, not `0.x d`; (d) boundary cases at each unit threshold. |
| Vitest tests — RAG ignores selection (D18) | `Lighthouse.Frontend/src/pages/Common/MetricsView/BaseMetricsView.test.tsx` (EXTEND) | EXTEND | Assert that with a picker selection active, the displayed bars come from the narrowed response BUT the RAG status is still computed from the systemic (`ctx.cumulativeStateTime`) response and does NOT change with the selection (DDD-23 / D18). |
| Vitest tests — `CumulativeStateTimeDrillDownDialog` | `Lighthouse.Frontend/src/components/Common/Charts/CumulativeStateTimeDrillDownDialog.test.tsx` | NEW | (a) Opens with `open: true`, closes on Escape, closes on outside click; (b) renders one row per item in default sort (daysContributed desc); (c) column sort toggling works; (d) Work Item ID renders as a `<Link>` and Enter activates it; (e) ARIA `role="dialog"` + `aria-labelledby` correctness; (f) empty case renders the US-04 empty-state message; (g) does NOT call any data-fetch hooks (data passed via props). |
| Vitest tests — `MetricsService` + `useMetricsData` | colocated `.test.ts` files | EXTEND | Mock `getCumulativeStateTime…` resolves; assert `ctx.cumulativeStateTime` populated; assert parallel-fetch shape; assert URL contains the correct query parameters. |
| Vitest tests — `ragRules.ts` | `Lighthouse.Frontend/src/pages/Common/MetricsView/ragRules.test.ts` | EXTEND | Add tests for `computeCumulativeStateTimeRag` covering all three RAG thresholds + empty-states case + single-state-100% edge case. Existing tests for other RAG rules pass unchanged. |
| Vitest tests — `categoryMetadata.ts` + `widgetInfoMetadata.ts` | colocated `.test.ts` files | EXTEND | Assert `getWidgetsForCategory("flow-metrics", "team")` and `getWidgetsForCategory("flow-metrics", "portfolio")` both include `stateTimeCumulative`. Assert `widgetInfoMetadata.stateTimeCumulative` has the expected fields. |
| NUnit tests — `BaseMetricsService.ComputeCumulativeStateTime` + `ComputeCumulativeStateTimeItems` | `Lighthouse.Backend/Lighthouse.Backend.Tests/Services/Implementation/BaseMetricsServiceTests.cs` (or sibling test file) | NEW / EXTEND | Fixture with: 5 items closed in window with known per-state visits; 2 items closed pre-window with no transitions in window (excluded by D12); 2 items in-flight throughout the window; 1 item just-started in-flight; 1 item that re-entered Review multiple times. Assert exact `completedContributionDays` + `ongoingContributionDays` + counts per state. Boundary tests: items entering before window + exiting in window (full duration counted), items exiting on day 1 (full pre-window duration counted), in-flight items with `currentStateEnteredAt` before window (full unclipped contribution). Determinism test: inject a fixed `nowSnapshot` and assert reproducibility. Empty case + zero-state case. Drill-down test: assert `Σ daysContributed = totalDays[state]` within ±0.1d for each state. Subset test (DDD-20): passing a narrowed `includedItems` produces bars summing only those items; single-item subset renders that item's per-state distribution (DDD-24). |
| NUnit tests — `TeamMetricsService.GetCumulativeStateTimeForTeam` + items + candidates variants | `Lighthouse.Backend/Lighthouse.Backend.Tests/Services/Implementation/TeamMetricsServiceTests.cs` | EXTEND | D12 inclusion-rule integration test against EF InMemory fixture: assert candidate set matches the expected union. `itemIds` intersection test (DDD-20): a selection narrows the contributing population; a selected id that is NOT in the D12 set is silently ignored (never smuggles an excluded item in — the intersection guarantees this). Candidates-endpoint test (DDD-19): assert `GetCumulativeStateTimeCandidatesForTeam` returns exactly the D12-included items for the window, each projecting `referenceId`/`title`/`workItemType`/`parentReferenceId`. Cache-key shape test: assert systemic key `CumulativeStateTime_{startDate:yyyy-MM-dd}_{endDate:yyyy-MM-dd}`, candidates key `CumulativeStateTime_Candidates_{startDate}_{endDate}`, items key `CumulativeStateTime_Items_{state}_{startDate}_{endDate}`, and that a non-empty `itemIds` selection produces a DISTINCT cache key (selection-hash suffix) so systemic and narrowed responses do not collide. Cache hit/miss behaviour test. |
| NUnit tests — `PortfolioMetricsService.GetCumulativeStateTimeForPortfolio` + items + candidates variants | `Lighthouse.Backend/Lighthouse.Backend.Tests/Services/Implementation/PortfolioMetricsServiceTests.cs` | EXTEND | Same coverage as Team (bar+`itemIds`, items+`itemIds`, candidates). Cross-scope parity test: identical fixture across team and portfolio scopes produces shape-identical responses for all three endpoints. |
| NUnit tests — `TeamMetricsController` + `PortfolioMetricsController` | `Lighthouse.Backend/Lighthouse.Backend.Tests/API/TeamMetricsControllerTest.cs` + `PortfolioMetricsControllerTests.cs` | EXTEND | HTTP 400 on inverted date range; HTTP 400 on missing/empty `state` for items endpoint; HTTP 200 with empty body on team with no items; HTTP 200 with expected DTO shape on populated fixture; HTTP 200 with empty `items: []` on unknown state name; `itemIds` ABSENT returns the systemic set (param is nullable — no 400, ci-learnings 2026-05-16); `itemIds` PRESENT (repeated query params, DDD-22) returns the narrowed set; candidates endpoint returns the D12-included items with parent references; `[RbacGuard]` test asserts unauthenticated returns 401/403 matching existing `cycleTimePercentiles` tests across all three new endpoints. |
| NUnit integration test — drill-down vs bar sum invariant | `Lighthouse.Backend/Lighthouse.Backend.Tests/API/TeamMetricsControllerTest.cs` (via `WebApplicationFactory`) | NEW | Calls both endpoints against the same fixture+window and asserts `Σ daysContributed = totalDays[state]` within ±0.1d for each state in the bar response. |
| ArchUnitNET test (NEW rule extension) | `Lighthouse.Backend/Lighthouse.Backend.Tests/Architecture/` (existing suite) | EXTEND | (a) Extends ADR-021 rule: no class or interface named `*PerStateAggregation*` exists in this feature's commit set (covering all three MVP-bundle features now). (b) Extends ADR-015 + ADR-021 rule: this feature's metrics-service classes read transitions only via `IWorkItemStateTransitionRepository`, never `DbSet<WorkItemStateTransition>` directly. (c) The two new `BaseMetricsService` helpers (`ComputeCumulativeStateTime`, `ComputeCumulativeStateTimeItems`) are `protected` (intra-inheritance) and NOT exposed via any interface. (d) This feature's services do NOT call sibling F's `ComputeAgeInStatePercentiles`. |
| E2E spec | `Lighthouse.EndToEndTests/tests/specs/flow/StateTimeCumulativeView.spec.ts` (new file) | NEW | 1 happy-path scenario: open team detail → Flow Metrics tab → see `CumulativeStateTimeChart` with bars → hover one bar and verify tooltip shows the fields + counts → click the tallest bar → verify drill-down dialog opens with items table → Escape closes dialog → open the item picker, select a feature's children via parent-expand, verify the bars recompute over the subset and the chart still renders → clear the picker → verify systemic view returns. Per-state arithmetic correctness lives in NUnit (faster, deterministic) per the established sibling-1 pattern. Keep the E2E minimal (memory: push implementation-invariant assertions to backend/Vitest); use seeded demo data, not live syncs (memory). |

### EXTEND / NEW summary

**EXTEND: 14** (existing files / classes / hooks / interfaces / metadata modules / test suites that gain additive code paths — now including the picker-aware `BaseMetricsView`, the `itemIds`/candidate-aware service+controller pairs, and the 6-method `MetricsService`).
**NEW: 12** at the file/component level: `CumulativeStateTimeDto.cs`, `CumulativeStateTimeItemsDto.cs`, `CumulativeStateTimeCandidatesDto.cs`, `CumulativeStateTimeChart.tsx`, `CumulativeStateTimeItemPicker.tsx`, `CumulativeStateTimeDrillDownDialog.tsx`, `formatDuration.ts`, `CumulativeStateTime.ts` + `CumulativeStateTimeItems.ts` + `CumulativeStateTimeCandidates.ts` (TS models), the new Vitest test files (chart, picker, formatDuration), the new E2E spec, and the new NUnit integration test for the sum invariant. (Counted at file granularity; records inside the DTO files are NEW types counted as part of the single new file.)
**REUSE AS-IS: 14** (`IWorkItemStateTransitionRepository` sibling-1 port, `IWorkItemRepository`, `WorkItem.CurrentStateEnteredAt` + `State` + `StateCategory`, `WorkItemBase.ParentReferenceId` [vendor-neutral, drives parent-expand], `BaseMetricsService.GetFromCacheIfExists`, `PercentileCalculator.CalculatePercentile`, `WorkItemBase.GetDateDifference`, MUI-X `<BarChart>` + `<ChartsContainer>` + `<ChartsTooltip>`, MUI `Autocomplete` + `Chip` [picker idiom from `ManualForecaster.tsx`], MUI `Dialog` + `DialogTitle` + `DialogContent`, `DataGridBase`, `WidgetShell`, `useRbac` hook, the existing `LogDateBoundaries` controller pattern, `GetEntityByIdAnExecuteAction` controller dispatch helper).

## Wave: DESIGN / [REF] Driving ports

| Method | Route | Auth | Status | Implementation pointer |
|---|---|---|---|---|
| GET | `/api/teams/{teamId:int}/metrics/cumulativeStateTime?startDate&endDate&itemIds` | `[RbacGuard(TeamRead)]` (existing class-level) | NEW | `TeamMetricsController.GetCumulativeStateTimeForTeam` — mirrors `cycleTimePercentiles` shape and validation (line 119). `itemIds` = `[FromQuery] int[]?` (nullable; repeated query params per DDD-22), absent ⇒ systemic view |
| GET | `/api/teams/{teamId:int}/metrics/cumulativeStateTime/items?state={stateName}&startDate&endDate&itemIds` | `[RbacGuard(TeamRead)]` | NEW | `TeamMetricsController.GetCumulativeStateTimeItemsForTeam` — additional `state` non-empty validation; optional `itemIds` narrows to the selected subset |
| GET | `/api/teams/{teamId:int}/metrics/cumulativeStateTime/candidates?startDate&endDate` | `[RbacGuard(TeamRead)]` | NEW (US-05) | `TeamMetricsController.GetCumulativeStateTimeCandidatesForTeam` — D12-included items for the window (DDD-19), feeds the picker; no `state`, no `itemIds` |
| GET | `/api/portfolios/{portfolioId:int}/metrics/cumulativeStateTime?startDate&endDate&itemIds` | `[RbacGuard(PortfolioRead)]` | NEW | `PortfolioMetricsController.GetCumulativeStateTimeForPortfolio` — mirror of Team endpoint (+ optional `itemIds`) |
| GET | `/api/portfolios/{portfolioId:int}/metrics/cumulativeStateTime/items?state={stateName}&startDate&endDate&itemIds` | `[RbacGuard(PortfolioRead)]` | NEW | `PortfolioMetricsController.GetCumulativeStateTimeItemsForPortfolio` — mirror of Team endpoint (+ optional `itemIds`) |
| GET | `/api/portfolios/{portfolioId:int}/metrics/cumulativeStateTime/candidates?startDate&endDate` | `[RbacGuard(PortfolioRead)]` | NEW (US-05) | `PortfolioMetricsController.GetCumulativeStateTimeCandidatesForPortfolio` — mirror of Team candidates endpoint |
| GET | `/api/teams/{teamId:int}/metrics/cycleTimePercentiles` | Existing | NO CHANGE (D9 of DISCUSS) | Unchanged |
| GET | `/api/teams/{teamId:int}/metrics/ageInStatePercentiles` | New-via-sibling-F | NO CHANGE | Unchanged; sibling F ships independently |

Six new endpoints (bar, drill-down items, picker candidates × team + portfolio). Consistent with the DISCUSS Driving-ports table. No new top-level routes. No premium gate. WS strategy Type A: additive throughout — the `itemIds` param is additive (absent ⇒ prior systemic behaviour), so adding it does not break any earlier-slice consumer.

Candidate response shape (both scopes, US-05):

```
{
  "items": [
    { "workItemId": 1234, "referenceId": "JIRA-1234", "title": "Investigate latency spike",
      "workItemType": "Bug", "parentReferenceId": "JIRA-1200" },
    ...
  ]
}
```

`referenceId` + `title` are the picker search keys (D14); `parentReferenceId` (vendor-neutral `WorkItemBase.ParentReferenceId`) drives parent-expand; `workItemId` is the numeric id echoed back as `itemIds`. Only the D12-included items for the window appear (D17).

Bar response shape (both scopes):

```
{
  "states": [
    {
      "state": "In Progress",
      "workflowOrder": 0,
      "totalDays": 184.0,
      "completedContributionDays": 142.0,
      "ongoingContributionDays": 42.0,
      "itemCount": 47,
      "completedItemCount": 38,
      "ongoingItemCount": 9,
      "meanDays": 3.9,
      "medianDays": 3.0
    },
    { "state": "Review", "workflowOrder": 1, ... },
    { "state": "Test", "workflowOrder": 2, ... }
  ]
}
```

Drill-down response shape (both scopes):

```
{
  "state": "Review",
  "items": [
    {
      "workItemId": "JIRA-1234",
      "title": "Investigate latency spike",
      "workItemType": "Bug",
      "currentState": "Review",
      "daysContributed": 42.0
    },
    ...
  ]
}
```

States are returned in workflow order (matching the chart's bar ordering). Zero-contributing states still appear with `totalDays: 0`; empty `states: []` when no items match. Items sorted by `daysContributed` desc; empty `items: []` when no contributors.

## Wave: DESIGN / [REF] Driven ports + adapters

| Port | Adapter | Status |
|---|---|---|
| `IWorkItemStateTransitionRepository` (extends `IRepository<WorkItemStateTransition>`) | `WorkItemStateTransitionRepository` (shipped by sibling 1, ADR-015) | REUSE AS-IS — consumed via `GetAllByPredicate` |
| `IWorkItemRepository` (extends `IRepository<WorkItem>`) | `WorkItemRepository` (existing) | REUSE AS-IS — used for D12 inclusion-rule candidate resolution |
| `WorkItem.CurrentStateEnteredAt` read access | Direct property on `WorkItem` (shipped by sibling 1, ADR-016) | REUSE AS-IS — read-only |
| `WorkItem.State` / `StateCategory` read access | Direct properties on `WorkItem` (existing) | REUSE AS-IS — read-only |
| `WorkItem.ParentReferenceId` read access (US-05 parent-expand, DDD-19) | Direct property on `WorkItemBase` (existing, vendor-neutral) | REUSE AS-IS — read-only. Confirmed populated across ALL connectors (Jira/ADO/Linear/CSV — each connector sets `ParentReferenceId`; `DemoDataFactory` seeds it; `WorkItemDto` already projects it as `ParentWorkItemReference`). Parent-expand works uniformly across connectors with no schema change. The candidate projection reads this field directly; no new driven port. |
| Cache: existing `BaseMetricsService.GetFromCacheIfExists` | Existing in-process cache (post-`bug-5016-cache-thread-safety` thread-safe) | REUSE AS-IS — new cache key namespaces `CumulativeStateTime_…`, `CumulativeStateTime_Items_…`, and `CumulativeStateTime_Candidates_…` slot into the same scope-keyed cache. A non-empty `itemIds` selection adds a selection-hash suffix so systemic and narrowed responses cache independently (DDD-20). |

The driven-port surface is UNCHANGED from the original DESIGN by the amend: still only the transitions read path + the work-item read path + the cache. US-05 adds NO new driven port — `parentReferenceId` for the picker candidates is read from the existing `WorkItemBase.ParentReferenceId` property, vendor-neutral across Jira/ADO/Linear/CSV.

External integrations: NONE introduced by this feature. The endpoints read only Lighthouse-internal persisted data (transitions table + work items table). The sibling 1 connector capture machinery is what populates the transitions table — this feature is purely a downstream reader. **No contract tests recommended** because no external integration is introduced.

## Wave: DESIGN / [REF] Technology choices

| Component | Pin |
|---|---|
| Backend | C# .NET 8, ASP.NET Core, EF Core 8.x (existing) |
| Backend tests | NUnit 4.6, Moq, EF InMemory, `Microsoft.AspNetCore.Mvc.Testing` (project_test_stack memory: project actually uses NUnit, not CLAUDE.md's xUnit claim) |
| Backend mutation | Stryker.NET (≥80% kill rate gate per CLAUDE.md) |
| Backend ArchUnit | ArchUnitNET (existing suite extended per ADR-024 rules) |
| Frontend | React 18 + TypeScript 5.x (strict), MUI 5.x, MUI-X-charts (existing chart library — no version change) |
| Frontend tests | Vitest + React Testing Library |
| Frontend mutation | Stryker (TS) (≥80% kill rate gate) |
| Frontend linter | Biome (zero errors / zero warnings on `./src` per CLAUDE.md) |
| E2E | Playwright with Page Object Model |

NO new technology introduced. NO new library dependency. NO new third-party service. The MUI-X `<BarChart>` primitive is already used elsewhere in the codebase (e.g. `BarRunChart.tsx`); the SVG `<pattern>` for hatching is standard SVG (no library); MUI `Dialog` + `DialogTitle` + `DialogContent` are already used by `WorkItemsDialog`, `FeedbackDialog`, `DeleteConfirmationDialog`, etc.; `DataGridBase` is the established table component; the US-05 item picker is built on MUI `Autocomplete` (`multiple`) + `Chip` — the SAME item/feature-selection idiom already used in `ManualForecaster.tsx` (no new dependency); the `formatDuration` adaptive-unit util is pure TypeScript (no library).

## Wave: DESIGN / [REF] Decisions table

| ID | Decision | Source / ADR |
|---|---|---|
| DDD-1 | D12 inclusion rule: UNION of transition-intersection AND in-flight-at-windowEnd | ADR-022 |
| DDD-2 | Per-visit duration: FULL `(exit - entry)` unclipped; visit-level primitive | ADR-022 |
| DDD-3 | In-flight duration: `nowSnapshot - currentStateEnteredAt` unclipped; single `now` per request | ADR-022 |
| DDD-4 | Segment split: completed = Σ visits; ongoing = Σ in-flight currents | ADR-022 |
| DDD-5 | Per-item drill-down: `daysContributed` = Σ visits + (in-flight if current state) | ADR-022 |
| DDD-6 | US-01 counts (reframed from US-03): completed + ongoing + distinct item-count (non-additive case documented in tooltip wording); counts stay in US-01 tooltip, explanation moves to widget-info (D13) | ADR-022 §6 + ADR-028 §1 |
| DDD-7 | Mean + median via `PercentileCalculator` for algorithmic parity | ADR-022 |
| DDD-8 | Workflow state order from team's `WorkflowStates`; Done aggregated into doneStates.First() | ADR-022 |
| DDD-9 | Empty / zero-state: empty `states: []` or `totalDays: 0` placeholder; FE handles presentation | ADR-022 |
| DDD-10 | Cache keys distinct from sibling F's namespace | ADR-022 |
| DDD-11 | Drill-down endpoint: SEPARATE endpoint (not `?expand=items`) | ADR-023 |
| DDD-12 | Drill-down UI primitive: MUI `Dialog` (following `WorkItemsDialog`) | ADR-023 |
| DDD-13 | Chart widget: NEW `CumulativeStateTimeChart` using MUI-X `<BarChart>` (not extension of `WorkItemAgingChart`) | ADR-025 |
| DDD-14 | Widget registration: single entry in `categoryMetadata` + `widgetInfoMetadata` + `ragRules` | ADR-025 |
| DDD-15 | ADR-018 + ADR-021 disposition: UPHELD (Path A-prime). No shared service. Three-way convergence | ADR-024 |
| DDD-16 | Endpoint route shape mirrors `cycleTimePercentiles` validation pattern | DESIGN mechanical, no ADR |
| DDD-17 | Auth via existing class-level `RbacGuard` (all six endpoints) | DESIGN mechanical, no ADR |
| DDD-18 | US-05 item picker: NEW `CumulativeStateTimeItemPicker` on MUI `Autocomplete`+`Chip`, chart-toolbar placement, Ref-ID/Name search, parent-expand as inline row action | ADR-028 §2 |
| DDD-19 | Picker candidate endpoint: NEW `candidates` endpoint per scope reusing the D12 query; projects `parentReferenceId` (vendor-neutral) | ADR-028 §3 |
| DDD-20 | `itemIds` subset filter: optional, intersected with the D12 set post-inclusion (never a bypass); systemic + narrowed cache independently | ADR-028 §3 |
| DDD-21 | Adaptive uniform display unit: NEW `formatDuration` util; FE chooses one unit from the largest bar; backend contract stays `totalDays` (double) | ADR-028 §4 |
| DDD-22 | `itemIds` transport: repeated GET query params bound as `[FromQuery] int[]?` (nullable); endpoints stay GET | ADR-028 §5 |
| DDD-23 | RAG ignores `itemIds` (D18): computed from the systemic (no-selection) response; the FE keeps the systemic data as the RAG source | ADR-028 §6 |
| DDD-24 | B2 absorption (D15): single-item selection renders the n=1 per-state distribution via the same arithmetic; chronology NOT built; PO persona/job added | ADR-028 §7 |

## Wave: DESIGN / [REF] Reuse Analysis

| Existing Component | File | Overlap | Decision | Justification |
|---|---|---|---|---|
| `BaseMetricsService` | `Lighthouse.Backend/Lighthouse.Backend/Services/Implementation/BaseMetricsService.cs` | Existing `protected` helpers for cycle-time / throughput / total-work-item-age / per-state percentile (sibling F's `ComputeAgeInStatePercentiles`); same inheritance pattern | EXTEND | Two new `protected` helpers (`ComputeCumulativeStateTime`, `ComputeCumulativeStateTimeItems`) live alongside the existing ones (ADR-024). Net add: ~40 LOC per helper. CREATE NEW service would violate ADR-018+021+024. |
| `TeamMetricsService` | `Lighthouse.Backend/Lighthouse.Backend/Services/Implementation/TeamMetricsService.cs` | Existing `GetCycleTimePercentilesForTeam` + `GetAgeInStatePercentilesForTeam` (from sibling F) provide the established pattern of "scope-specific item-resolution query + delegate to base helper + wrap in cache" | EXTEND (pattern) + two new methods | New methods follow the same structural shape; D12 inclusion-rule resolution is new code (transition-intersection + in-flight query) but reuses `IWorkItemRepository` + `IWorkItemStateTransitionRepository` already extant. CREATE NEW service would duplicate scaffolding. |
| `PortfolioMetricsService` | `Lighthouse.Backend/Lighthouse.Backend/Services/Implementation/PortfolioMetricsService.cs` | Mirror of Team service | EXTEND | Same reasoning. |
| `TeamMetricsController` | `Lighthouse.Backend/Lighthouse.Backend/API/TeamMetricsController.cs:119` | Existing `cycleTimePercentiles` endpoint provides exact validation + dispatch shape | EXTEND (pattern) + two new endpoints | Two new endpoints follow `cycleTimePercentiles` shape exactly — same validation, same error message, same `GetEntityByIdAnExecuteAction` dispatch, same `LogDateBoundaries` call. CREATE NEW controller would duplicate scaffolding. |
| `PortfolioMetricsController` | `Lighthouse.Backend/Lighthouse.Backend/API/PortfolioMetricsController.cs` | Mirror of Team controller | EXTEND (pattern) + two new endpoints | Same reasoning. |
| `BaseMetricsService.GetFromCacheIfExists` | `Lighthouse.Backend/Lighthouse.Backend/Services/Implementation/BaseMetricsService.cs:378` | Existing cache mechanism, thread-safe post-bug-5016 | REUSE AS-IS | New cache-key namespaces (`CumulativeStateTime_…` and `CumulativeStateTime_Items_…`) slot alongside existing `CycleTimePercentiles_…` and `AgeInStatePercentiles_…`. No mechanism change. |
| `PercentileCalculator.CalculatePercentile` | `Lighthouse.Backend/Lighthouse.Backend/Services/Implementation/PercentileCalculator.cs` | Existing nearest-rank algorithm; used by `cycleTimePercentiles` and sibling F's `ageInStatePercentiles` | REUSE AS-IS | Used for median-per-state computation in ADR-022 §7 — algorithmic parity with the two sibling endpoints. |
| `IWorkItemStateTransitionRepository` | `Lighthouse.Backend/Lighthouse.Backend/Services/Interfaces/Repositories/IWorkItemStateTransitionRepository.cs` (sibling 1, ADR-015) | Exposes `GetAllByPredicate(Expression<Func<WorkItemStateTransition, bool>>)` via `IRepository<T>` | REUSE AS-IS | The transition-fetcher delegate `id => transitionRepo.GetAllByPredicate(t => t.WorkItemId == id)` is consumed by the new helpers. Adding methods to the repository was explicitly deferred to consumer DESIGNs per ADR-015; this DESIGN does NOT add methods — the base `GetAllByPredicate` is sufficient. |
| `IWorkItemRepository` + `GetAllByPredicate` | `Lighthouse.Backend/Lighthouse.Backend/Services/Interfaces/Repositories/IWorkItemRepository.cs` | Existing repository for work-item queries | REUSE AS-IS | D12 inclusion-rule candidate resolution composes two predicates (transition-intersection and in-flight-at-windowEnd) via `GetAllByPredicate`. No new methods. |
| `WorkItem.CurrentStateEnteredAt` | `Lighthouse.Backend/Lighthouse.Backend/Models/WorkItemBase.cs` (sibling 1 ADR-016) | Persisted column on `WorkItem`; read by sibling F too | REUSE AS-IS | Read-only access; D12 (b) clause and in-flight duration computation both read this field. No mutation. |
| `WorkItem.State` / `WorkItem.StateCategory` | `Lighthouse.Backend/Lighthouse.Backend/Models/WorkItemBase.cs` | Existing fields | REUSE AS-IS | D12 (b) clause requires `StateCategory != Done`; in-flight contribution attributes to `W.State`. |
| `WorkItemBase.GetDateDifference` | `Lighthouse.Backend/Lighthouse.Backend/Models/WorkItemBase.cs` | Existing day-counting convention (date-only diff, ceiling) | REUSE AS-IS | Used by all per-visit + in-flight day calculations. Consistency with `CycleTime`, `WorkItemAge`, `ageInStatePercentiles`. |
| `LogDateBoundaries` controller helper | `Lighthouse.Backend/Lighthouse.Backend/API/TeamMetricsController.cs:355` | Existing debug-level logging convention | REUSE AS-IS | New endpoints call `LogDateBoundaries("cumulativeStateTime", teamId, startDate, endDate)` consistent with existing convention. |
| `GetEntityByIdAnExecuteAction` controller dispatch | Extension method used throughout `*MetricsController` | Existing controller dispatch pattern | REUSE AS-IS | New endpoints dispatch via `this.GetEntityByIdAnExecuteAction(teamRepository, teamId, team => teamMetricsService.GetCumulativeStateTime…)`. Same pattern as every other endpoint in the controller. |
| `MetricsService.ts` / `IMetricsService` (TS) | `Lighthouse.Frontend/src/services/Api/MetricsService.ts:247` | Existing `getCycleTimePercentiles(id, startDate, endDate)` provides exact HTTP shape | EXTEND | Four new methods following `getCycleTimePercentiles` exactly. CREATE NEW service would duplicate HTTP plumbing and date-format helpers. |
| `useMetricsData` (hook) | `Lighthouse.Frontend/src/hooks/useMetricsData.ts` | Existing parallel-fetch pattern for `cycleTimePercentiles` + (sibling F) `ageInStatePercentiles` | EXTEND | One additional parallel fetch in the same effect; one returned ctx field. CREATE NEW hook would duplicate window-state management. |
| `BaseMetricsView` | `Lighthouse.Frontend/src/pages/Common/MetricsView/BaseMetricsView.tsx` | Renders all widgets via `widgetKey`-based dispatch shared between team and portfolio routes | EXTEND | One new widget-dispatch entry; one local-state holder for the drill-down dialog. CREATE NEW view would split scope routing. |
| MUI `Dialog` + `DialogTitle` + `DialogContent` | `@mui/material` (existing dependency) | Used by `WorkItemsDialog`, `FeedbackDialog`, `DeleteConfirmationDialog`, etc. (11 existing dialog files confirmed via glob) | REUSE AS-IS | The drill-down dialog uses these components directly per ADR-023. Zero new library dependency. |
| `DataGridBase` | `Lighthouse.Frontend/src/components/Common/DataGrid/DataGridBase.tsx` | Existing table component with sorting, keyboard navigation, ARIA roles | REUSE AS-IS | The drill-down dialog's per-item table is a `DataGridBase` instance with column definitions per US-04 AC. Same usage pattern as `WorkItemsDialog`. |
| MUI-X `<BarChart>` + `<ChartsContainer>` + `<ChartsTooltip>` | `@mui/x-charts` (existing dependency) | Used by `BarRunChart.tsx` and sibling F's chart-overlay technique | REUSE AS-IS | The new chart uses `<BarChart>` natively with stacked horizontal series and a custom tooltip slot. Zero new chart library. |
| `WidgetShell` (loading/empty-state shell) | `Lighthouse.Frontend/src/pages/Common/MetricsView/WidgetShell.tsx` | Existing widget wrapper providing loading skeleton + error boundary + empty-state container | REUSE AS-IS | The chart wraps in `WidgetShell` like every other Flow Metrics widget. No custom shell. |
| `categoryMetadata.ts` | `Lighthouse.Frontend/src/pages/Common/MetricsView/categoryMetadata.ts` | Existing widget catalog | EXTEND | One new entry. Pattern verified at line 69-78 of the file (`flow-metrics` already contains 8 entries with the same shape). |
| `widgetInfoMetadata.ts` | `Lighthouse.Frontend/src/pages/Common/MetricsView/widgetInfoMetadata.ts` | Existing widget descriptions + RAG guidance + learn-more URLs | EXTEND | One new entry. Pattern established. |
| `ragRules.ts` | `Lighthouse.Frontend/src/pages/Common/MetricsView/ragRules.ts` | Existing RAG-function pattern (`computeWipOverviewRag`, `computeBlockedOverviewRag`, etc.) | EXTEND | One new function. Same shape — returns `{ ragStatus, tipText }`. Established convention. |
| `useRbac` hook | `Lighthouse.Frontend/src/services/RbacContext.tsx` | Existing RBAC state source | REUSE AS-IS | No new gating needed — the new endpoints are Authenticated (TeamRead / PortfolioRead via existing class-level `RbacGuard`); no premium gate. `useRbac` consumption follows the existing widget pattern. |
| MUI `Autocomplete` + `Chip` (US-05 picker) | `@mui/material` (existing); pattern at `Lighthouse.Frontend/src/pages/Teams/Detail/ManualForecaster.tsx:263` (`Autocomplete` with `getOptionLabel`/`onChange`/`renderInput` + `Chip` selected-tags) | The picker is a multi-select-with-search-and-chips control; `ManualForecaster` already does single-select item/feature pickup with `Autocomplete`+`Chip` | REUSE the idiom, CREATE NEW component | The literal `ManualForecaster` Autocomplete is single-select (`value={null}`, `blurOnSelect`), feature-scoped, and forecast-coupled — wrong concern to extend. But the `Autocomplete`(`multiple`)+`Chip` PATTERN is the established codebase idiom; the new `CumulativeStateTimeItemPicker` reuses that idiom with `multiple` + a custom `filterOptions` (Ref-ID/Name only, D14) + parent-expand. Zero new dependency. EXTENDING `ManualForecaster` directly would couple the chart picker to the forecaster's feature model — rejected. |
| `WorkItemBase.ParentReferenceId` (US-05 parent-expand) | `Lighthouse.Backend/Lighthouse.Backend/Models/WorkItemBase.cs:22` (also projected as `WorkItemDto.ParentWorkItemReference`) | Parent-expand needs each candidate's parent reference; this field already exists and is connector-neutral | REUSE AS-IS | Populated by every connector (Jira/ADO/Linear/CSV grep confirmed) + `DemoDataFactory`. The candidate projection reads it directly. No new field, no schema change, no new connector work — parent-expand is free across all work-tracking systems. |
| `date/age.ts` + `charts/chartAxisUtils.ts` (duration formatting) | `Lighthouse.Frontend/src/utils/date/age.ts`, `Lighthouse.Frontend/src/utils/charts/chartAxisUtils.ts` | The adaptive-unit `formatDuration` (DDD-21) is a duration→string formatter; these are the existing date/number formatters | CREATE NEW (`formatDuration.ts`) | `age.ts` returns a day-COUNT (`number`), no unit string; `chartAxisUtils.ts` formats raw numbers/dates with no unit-laddering. Neither chooses minutes→hours→days→weeks from a magnitude. Extending either would distort its single responsibility. NEW `formatDuration.ts` co-located in `utils/date/` beside `age.ts`. Pure util, isolated, mutation-tested. |
| `FilterBar` (search control) | `Lighthouse.Frontend/src/components/Common/FilterBar/FilterBar.tsx` | Both are "search" controls | CREATE NEW (picker) — NOT extend FilterBar | `FilterBar` is a single free-text search box that emits a filter STRING for the overview list; it has no multi-select, no chips, no candidate list, no parent-expand, and is bound to team/portfolio-name search. The picker needs a structured multi-select over a candidate set. Wrong shape to extend; the `Autocomplete`+`Chip` idiom is the right reuse, not `FilterBar`. |

**Totals: 31 rows. N EXTEND = 14, M REUSE-AS-IS = 14, K CREATE-NEW (with rejected-extend justification) = 3 OVERLAP rows (`formatDuration` vs age/chartAxis; picker vs ManualForecaster-Autocomplete + vs FilterBar — the picker counts once, evaluated against two extend candidates).** The NEW files in the Component decomposition table (12 new files) all have ZERO existing semantic duplicate per the codebase greps below; the three OVERLAP rows above record the closest existing components and why each is reused-as-idiom or create-new rather than extended.

CODEBASE GREP FOR OVERLAP CANDIDATES THAT MIGHT JUSTIFY CREATE-NEW BUT WERE REJECTED:

- Searched for `CumulativeStateTime|cumulativeStateTime|StateCumulative|stateCumulative|StateTimeBar|stateTimeBar` across `Lighthouse.Backend` + `Lighthouse.Frontend/src` — zero production matches (confirmed via pre-DISCUSS code reality check; reconfirmed at DESIGN and at this amend). No existing DTO, service, hook, or component duplicates the per-state cumulative concern.
- Searched for `PerState|StateAggregation|PerStateMetric` — zero production matches (extends sibling F's finding). Confirms ADR-018+021+024 "no existing service to extend" position remains true.
- Searched for `*Drawer*.tsx` and `*SidePanel*.tsx` under `Lighthouse.Frontend/src/` — zero matches. Confirms ADR-023's "Dialog is the only in-codebase precedent" finding (drill-down modal-vs-side-panel already settled in ADR-023 — modal).
- Searched for `BarChart` usage — `BarRunChart.tsx` already imports `@mui/x-charts` BarChart for a different chart shape (single-series, vertical, time-axis). The MUI-X primitive is established; this DESIGN reuses the same import without re-engineering the chart library integration.
- Searched for `Autocomplete|MultiSelect|ItemPicker|FilterBar` under `Lighthouse.Frontend/src` — `ManualForecaster.tsx` (single-select item/feature Autocomplete+Chip) and `FilterBar.tsx` (free-text overview search) are the closest; both evaluated in the OVERLAP rows above (reuse the Autocomplete idiom, create a new component — neither is the right thing to extend literally).
- Searched for `formatDuration|formatDays|humanize|duration` under `Lighthouse.Frontend/src` — no adaptive/unit-laddering duration formatter exists; the matches (`RefreshHistoryChart`, `CycleTimePercentiles`, `RefreshLog`) format fixed units or raw numbers. `formatDuration.ts` is genuinely new.
- Searched for `ParentReferenceId|ParentWorkItemReference` across `Lighthouse.Backend` — confirmed on `WorkItemBase` + every connector + `WorkItemDto` + `DemoDataFactory`. Parent-expand reuses it; no new field.

## Wave: DESIGN / [REF] Open questions

### Proposed choices from the 2026-05-26 amend (PROPOSE mode — recommended, pending user confirm)

> The amend introduced five open choices (A–E). Each is RESOLVED with a recommendation that is APPLIED to the design above and marked "(recommended — pending user confirm)" so the user can override. Trade-offs are kept to a few lines each.

- **A. US-04 drill-down panel — modal vs side-panel**: **ALREADY RESOLVED — MUI `Dialog` modal (ADR-023).** This was deferred from DISCUSS to DESIGN and decided in the 2026-05-24 DESIGN pass: zero `Drawer`/`SidePanel` precedent in the codebase; `WorkItemsDialog` is the established "table-from-chart-click" pattern; `DataGridBase` supplies table mechanics. The amend does NOT reopen it. *(No further user input needed unless they want to overturn ADR-023.)*
- **B. US-05 picker UI — placement and form** *(recommended — pending user confirm)*: **chart-toolbar `Autocomplete`(`multiple`)+`Chip` control, parent-expand as an inline "Select all N children" row action.** Options weighed: (1) chart-toolbar multi-select [chosen — visually bound to the chart it scopes, reuses the `ManualForecaster` idiom, keyboard/SR-accessible via Autocomplete's combobox ARIA]; (2) standalone dropdown menu [rejected — divorces the control from the chart and needs its own ARIA scaffolding]; (3) dedicated side panel [rejected — heavier, no codebase precedent, same Drawer-absence finding as ADR-023]. Parent-expand as a tree control [rejected — over-engineered; the inline row action covers "all children of a feature" in one click].
- **C. `itemIds` transport** *(recommended — pending user confirm)*: **repeated GET query params, bound `[FromQuery] int[]?`.** Options weighed: (1) repeated params [chosen — idiomatic ASP.NET array binding, GET stays cacheable + REST-consistent with the existing metrics endpoints, small in practice — a feature's children or a handful of items]; (2) comma-separated single param [rejected — manual parse, no model-binder support, no real URL-length win]; (3) switch to POST [rejected for MVP — breaks GET caching and the uniform `cycleTimePercentiles` controller shape for a payload that is small in practice]. Follow-up: if a future bulk-select feature blows the ~2 KB URL budget, switch THAT endpoint to POST (documented in DDD-22).
- **D. Shared per-state aggregation service (D10) — extract now vs stay independent** *(recommended — pending user confirm)*: **STAY INDEPENDENT — uphold ADR-018+021+024.** With sibling F shipped and this feature's six endpoints + sibling F's per-state percentile endpoint all computing per-state aggregations, the question is revisited and the answer is unchanged: a shared `IPerStateAggregationService` carries the documented three-way conflation risk (sibling F = ClosedDate-in-window completed-only; this feature = D12 frame-intersection-OR-in-flight + full-duration unclipped + segment-split), the cache-sharing win is zero at MVP scale, and supersession would break three stable ADRs across three feature boundaries. The two `protected` helpers stay colocated in `BaseMetricsService` alongside `ComputeAgeInStatePercentiles`. The post-MVP extraction path (semantically-named methods that embed the inclusion rule in the name) remains documented in ADR-024. **Reasoning: the divergence is now MORE concrete after the amend (the `itemIds` subset narrowing is unique to this feature; sibling F has no such notion), reinforcing the keep-separate decision rather than weakening it.**
- **E. `formatDuration` util — reuse/extend vs new** *(recommended — pending user confirm)*: **CREATE NEW at `Lighthouse.Frontend/src/utils/date/formatDuration.ts`.** No existing util ladders minutes→hours→days→weeks from a magnitude (`age.ts` returns day-counts; `chartAxisUtils.ts` formats raw numbers/dates). Co-located beside `age.ts` in `utils/date/`. Pure, isolated, mutation-tested. Produces a uniform unit per chart chosen from the largest bar (DDD-21).

### Remaining DELIVER-time validation spikes (carried forward, unchanged by the amend)

- **MUI-X `<BarChart>` per-bar `onClick` API granularity**: ADR-025 assumes MUI-X 5+ exposes per-bar click events that yield the clicked bar's data/index (the property name varies across MUI-X versions: `onItemClick`, `onBarClick`, `slotProps.bar.onClick`, etc.). **Validation deferred to DELIVER slice 03 spike (~30 min)**: at the start of slice 03 implementation, identify the correct event handler shape for the project's pinned MUI-X version. If the per-bar click API does not exist or is not exposed at the expected level, the alternative is to wrap each bar's SVG `<rect>` in a click handler via a custom MUI-X chart child (ADR-020 demonstrates this works for `<line>` overlays; the same technique applies to invisible click-target rects). No DESIGN blocker; the fallback is the established overlay technique sibling F already uses.
- **EF-friendly D12 inclusion-rule query shape**: ADR-022's pseudo-code expresses the D12 union as two separate predicate queries (transition-intersection candidates + in-flight candidates) plus a C# union. Software-crafter at GREEN may discover that a single LINQ expression composing both rules via `WorkItemRepository.GetAllByPredicate(w => (transitionRepo.GetAllByPredicate(t => t.WorkItemId == w.Id && t.TransitionedAt <= windowEnd).Any() && transitionRepo.GetAllByPredicate(t => t.WorkItemId == w.Id && t.TransitionedAt >= windowStart).Any()) || (w.StateCategory != Done && w.CurrentStateEnteredAt <= windowEnd))` translates poorly under EF Core 8 (Sqlite + Postgres lockstep). **Resolution**: the two-step C# union is the documented baseline; if the single-LINQ shape is acceptable under both providers at slice-01 implementation, software-crafter may pick it. Either shape is correct against ADR-022 §1.
- **Endpoint profiling at scale**: ADR-022's algorithm is `O(transitions_per_item × included_items)` for the bar endpoint, plus `O(items + transitions)` for the D12 candidate resolution. Expected sub-100ms uncached at MVP scale. **Validation deferred to DELIVER slice 01 spike (~30 min, parallel to sibling F's profiling spike)**: profile against the project's own ADO instance with 6 months of transition data. The existing `GetFromCacheIfExists` already handles repeat requests; a one-step materialisation (e.g. precompute per (team, window) at sync time) is a follow-up — NOT in scope for MVP.
- **Bar layout orientation (horizontal vs vertical)**: ADR-025 leaves the chart's MUI-X layout (`horizontal` with category-Y / value-X, or `vertical` with category-X / value-Y) as a software-crafter choice at GREEN, since DISCUSS D3 only mandates "workflow-order" not orientation. Both orientations satisfy D3. **Resolution**: pick the orientation that better fits the typical Flow Metrics widget area (currently `size: "large"` is wider than tall on desktop). The slice-01 dogfood screenshot will confirm.
- **Hatching pattern visual tuning**: SVG `<pattern>` dimensions (line spacing, stroke width, angle) are visual-tuning constants. ADR-025 specifies "diagonal hatching at 45°"; exact spacing is a slice-01 visual decision. Constraint: colour-blind-friendly per DISCUSS D6 (rejected "different shade" alternative).
- **Backfill of pre-feature transitions**: explicitly OUT OF SCOPE per DISCUSS (forward-only constraint inherited from sibling 1). First weeks post-release will have low / sparse bars for most states. The tooltip + RAG rule + empty-state messaging surface this honestly. **Resolved by DISCUSS**; no DESIGN action.
- **Parent-expand candidate scoping (US-05)**: parent-expand offers "select all children of a feature" only over the items ALREADY in the candidate set (the D12-included window set, D17). A child item not touched in the window is not selectable even if its parent is matched — selecting the parent expands to the IN-WINDOW children only. **Resolved by DDD-19** (candidates = the D12 set; parent-expand adds child ids drawn from that same set). The picker shows the child count it will actually add so the user is not surprised when an out-of-window child is absent. No DESIGN blocker; flagged so the slice-04 implementer does not reach for a separate "all children regardless of window" query.
- **Candidate-endpoint caching granularity**: the `candidates` endpoint caches per (scope, window) under `CumulativeStateTime_Candidates_{startDate}_{endDate}` — it does NOT vary by `itemIds` (it has no `itemIds` param; it always returns the full candidate set for the window). **Resolved by DDD-19**; software-crafter picks the exact cache-key sanitisation at GREEN as for the other two keys.

## Wave: DESIGN / [REF] Cross-MVP coordination outcomes

- **ADR-018 (sibling 1) + ADR-021 (sibling F) disposition: UPHELD via ADR-024 (this DESIGN's Path A-prime).** This DESIGN does NOT introduce `IPerStateAggregationService`. Per-state cumulative computation lives as two sibling `protected` helpers inside `BaseMetricsService` (`ComputeCumulativeStateTime`, `ComputeCumulativeStateTimeItems`), parallel to sibling F's `ComputeAgeInStatePercentiles`. Three independent DESIGN re-litigations across three sibling features in the same MVP bundle converge on the same conclusion. Rationale captured in ADR-024 with explicit reference to ADR-018, ADR-021, and ADR-022's concrete divergence documentation.
- **No upstream impact on sibling 1 DESIGN.** D9 of DISCUSS held: no schema additions to `WorkItemStateTransition` or `WorkItem`. ADR-022's algorithm reads only the 4 fields shipped by sibling 1 plus `WorkItem.CurrentStateEnteredAt` (sibling 1 ADR-016, read-only) plus existing `WorkItem.State` / `StateCategory` (read-only). Sibling 1 DESIGN remains final.
- **No upstream impact on sibling F DESIGN.** ADR-021 stays Accepted. ADR-024 (this DESIGN) explicitly UPHOLDS ADR-021 rather than superseding it. The two sibling features each ship their own `protected` helper inside `BaseMetricsService`, parallel-named, parallel-located, semantically distinct, never calling each other. Sibling F DESIGN remains final.
- **ArchUnitNET rule extension**: the existing rule (no class/interface named `*PerStateAggregation*`) introduced by ADR-021 EXTENDS to cover this feature's commit set. No new ArchUnit predicate; the rule's covered scope widens. Documented in ADR-024 enforcement table.
- **Semantic divergence permanent across the three siblings**: sibling 1 (foundation, no aggregation), sibling F (completed-items-only + ClosedDate-in-window + visit-level percentile distribution), this feature (frame-intersection OR in-flight-at-windowEnd + full-duration unclipped + segment-split sum + per-item drill-down). Three feature-deltas, three ADRs (019, 022), three distinct endpoint route prefixes (`/cycleTimePercentiles` existing, `/ageInStatePercentiles` new-via-sibling-F, `/cumulativeStateTime` new-via-this), three distinct cache-key namespaces. The divergence is not just documented; it is enforced by route, by service-method name, by cache-key shape, and by ArchUnitNET rule.
- **DEFAULT EXPECTATION HELD: no flag-ups for sibling DESIGNs.** Both sibling DESIGNs remain final and unmodified. Their feature-delta files do NOT require any post-hoc edits as a result of this DESIGN; this feature's ADR-024 references them but does not modify them.

## Wave: DESIGN / [REF] Wave decisions summary

> **AMENDED 2026-05-26** for D13–D18. Net delta from the 2026-05-24 DESIGN: +2 endpoints per scope is now +3 (added `candidates`); the bar + items endpoints gain an optional `itemIds` param; +1 NEW frontend component (`CumulativeStateTimeItemPicker`); +1 NEW util (`formatDuration`); +1 NEW DTO/model pair (candidates); the chart gains picker integration + the adaptive unit + the US-03-line removal; RAG is pinned to the systemic set (D18). New ADR-028 carries the amend decisions; ADR-022/023/024/025 unchanged.

**Primary architectural commitment**: introduce SIX new HTTP endpoints (three per scope: bar + drill-down items + picker candidates), two new `protected` helpers inside the existing `BaseMetricsService` (parallel to sibling F's pattern), three new frontend components (a NEW chart widget using MUI-X `<BarChart>`, a NEW drill-down dialog using MUI `Dialog`, and a NEW item picker using MUI `Autocomplete`+`Chip`), one new pure util (`formatDuration`, adaptive unit), one new RAG function, three new metadata-module entries (`categoryMetadata`, `widgetInfoMetadata`, `ragRules`), and six new TypeScript model interfaces (state-row, items, candidates × response+row). Every other touchpoint is an additive extension of an existing class / hook / component / DTO / cache key namespace. NO new top-level routes. NO new external integration. NO new external library. NO new persistence (no new EF migration). NO premium gate. NO breaking change — the `itemIds` param is optional (absent ⇒ prior systemic behaviour).

**Architecture pattern**: ports-and-adapters / hexagonal (unchanged). Reads transitions via sibling 1's repository port; reads work items via the existing repository port; reads `WorkItemBase.ParentReferenceId` (existing, vendor-neutral) for parent-expand; no new port introduced. The computation helpers live as `protected` methods inside `BaseMetricsService` — intra-inheritance, NOT exposed via interface, parallel to sibling F's `ComputeAgeInStatePercentiles` (ADR-024, UPHELD by the amend per Open-question D). The `itemIds` subset narrowing and the candidate projection live in the derived `Team`/`Portfolio` services (they own the D12 query); the base helpers stay subset-agnostic.

**Walking skeleton path** (validates the architecture in one slice end-to-end): existing sibling-1 `WorkItemStateTransition` rows + existing `WorkItem.CurrentStateEnteredAt` → new `BaseMetricsService.ComputeCumulativeStateTime` helper → new `TeamMetricsService.GetCumulativeStateTimeForTeam` method (with D12 candidate query + cache wrap) → new `TeamMetricsController.GetCumulativeStateTimeForTeam` endpoint → new `MetricsService.getCumulativeStateTimeForTeam` TS method → extended `useMetricsData` ctx → new `CumulativeStateTimeChart` (with adaptive `formatDuration` unit) rendering on the team detail page. Slice 01 exercises every architectural seam end-to-end. Slice 02 adds portfolio parity (US-03 withdrawn per D13 — no tooltip-enrichment story). Slice 03 adds US-04 per-item drill-down endpoints + dialog. Slice 04 adds US-05 item picker + `candidates` endpoint + `itemIds` wired through the bar + drill-down endpoints (the absorbed-B2 distribution lens, D15).

**Sibling MVP coordination**: ADR-018 + ADR-021 UPHELD via ADR-024 (three-way convergence). Semantic divergence vs sibling F documented permanently in ADR-022 (cross-feature impact section). Sibling 1's DESIGN unchanged (no schema feedback, no port feedback); sibling F's DESIGN unchanged (no helper-sharing, no ADR supersession).

**Downstream changes propagated upstream**: NONE. Sibling 1 DESIGN unchanged. Sibling F DESIGN unchanged.

**Handoff readiness**: ready for nw-acceptance-designer (DISTILL) handoff. The 4 user stories with their AC are already locked at DISCUSS; this DESIGN adds the architectural decomposition the acceptance designer needs to write executable specs against. Subsequent nw-platform-architect (DEVOPS) handoff: no CI workflow changes required (the existing test gates cover the new code; the existing `ci_verifysqlite.yml` + `ci_verifypostgres.yml` cover the no-new-migration case); no contract tests recommended (no external integrations).

**Modal-vs-side-panel decision** (deferred from DISCUSS US-04 to DESIGN; amend choice A): MUI `Dialog` modal chosen authoritatively in ADR-023. Rationale: zero `Drawer`/`SidePanel` precedent in the codebase; `WorkItemsDialog` is the established "table-from-chart-click" pattern; `DataGridBase` provides table mechanics; consistency with `FeedbackDialog`, `DeleteConfirmationDialog`, `WipSettingDialog`, etc. (11 existing dialog files). No further slice-work spike needed. The amend does not reopen this.

**Amend choices (A–E) disposition**: A (drill-down modal) = resolved in ADR-023, unchanged. B (picker UI = chart-toolbar Autocomplete+Chip, parent-expand inline row action), C (`itemIds` = repeated GET params, `int[]?`), D (shared aggregation = stay independent, ADR-018+021+024 upheld), E (`formatDuration` = new util in `utils/date/`) — all recorded in ADR-028 and the Open-questions section, each marked "(recommended — pending user confirm)". All five are APPLIED to the design above; the user may override any of B/C/D/E (A would require overturning ADR-023).

## Wave: DESIGN / [REF] Outcome Collision Check

`nwave-ai outcomes check-delta docs/feature/state-time-cumulative-view/feature-delta.md` was re-attempted at the 2026-05-26 amend and again NOT executed: the tool (`nwave-ai`) is not installed in this repository and the canonical outcomes registry at `docs/product/outcomes/registry.yaml` does not exist. Per skill ("skip-and-document"): documented here, deferred to DEVOPS handoff for KPI / outcomes registration alongside the DISCUSS-defined `OUT-cumulative-state-time-adoption`, `OUT-cumulative-state-time-leadership-share`, `OUT-cumulative-state-time-constraint-naming`, and `OUT-cumulative-state-time-item-filter-usage` (the US-05 picker-adoption KPI added in the 2026-05-26 revision). No collision possible to detect without the registry; flagged for the user.

