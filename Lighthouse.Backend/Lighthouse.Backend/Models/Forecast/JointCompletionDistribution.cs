namespace Lighthouse.Backend.Models.Forecast
{
    // __SCAFFOLD__ RED scaffold for Story #5569 (ADR-110, DDD-1/DDD-2). DELIVER replaces the body.
    internal static class JointCompletionDistribution
    {
        public static Dictionary<int, int> Combine(IEnumerable<IReadOnlyDictionary<int, int>> histograms)
        {
            throw new InvalidOperationException($"__SCAFFOLD__ {nameof(JointCompletionDistribution)}.{nameof(Combine)} is not implemented yet (Story #5569). Contributors: {histograms.Count()}");
        }
    }
}
