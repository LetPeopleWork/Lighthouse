@page
@model CMFTAspNet.Pages.Projects.IndexModel

@{
    ViewData["Title"] = "Projects";
}

<h1>Projects</h1>

<p>
    <a asp-page="Create">Create New Project</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Projects[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Projects[0].SearchBy)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Projects[0].WorkItemTypes)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Projects[0].SearchTerm)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Projects[0].TargetDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Projects[0].IncludeUnparentedItems)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Projects) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SearchBy)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WorkItemTypes)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SearchTerm)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TargetDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IncludeUnparentedItems)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
